import PlaceholderScreen from "./PlaceholderScreen";

// Account tab wrapper — PlaceholderScreen until Phase 6 fills it in.
// The wrapper exists so the tab structure never changes.
export default function AccountScreen() {
  return <PlaceholderScreen />;
}

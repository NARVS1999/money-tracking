---
phase: 03-entries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entries/EntriesProvider.tsx
  - src/components/EntryRow.tsx
  - src/components/DateSectionHeader.tsx
  - src/screens/ExpensesScreen.tsx
  - src/screens/IncomeScreen.tsx
  - App.tsx
autonomous: true
requirements:
  - ENTR-01
  - ENTR-05

must_haves:
  truths:
    - "EntriesProvider exposes entries array scoped to current user.uid via onSnapshot"
    - "EntriesProvider re-subscribes when user.uid changes and unsubscribes on sign-out"
    - "ExpensesScreen shows a FlatList of expense entries grouped by date with section headers"
    - "IncomeScreen shows a FlatList of income entries grouped by date with section headers"
    - "Empty state shows 'No entries yet' with tab-specific CTA text"
    - "Each entry row displays category name, amount (formatted with ₱), and date+description"
    - "FAB (+) button visible on both tabs, positioned above tab bar"
  artifacts:
    - path: src/entries/EntriesProvider.tsx
      provides: "Entries context provider with onSnapshot, addEntry, updateEntry, deleteEntry, copyEntry"
    - path: src/components/EntryRow.tsx
      provides: "Single entry row component with category, amount, date+description"
    - path: src/components/DateSectionHeader.tsx
      provides: "Sticky section header with Today/Yesterday/date labels"
    - path: src/screens/ExpensesScreen.tsx
      provides: "Expense entries list screen"
    - path: src/screens/IncomeScreen.tsx
      provides: "Income entries list screen"
  key_links:
    - from: src/entries/EntriesProvider.tsx
      to: src/firebase/queries.ts
      via: "entriesByType(uid, type) query for onSnapshot listener"
      pattern: "entriesByType"
    - from: src/screens/ExpensesScreen.tsx
      to: src/entries/EntriesProvider.tsx
      via: "useEntries() hook for entries data"
      pattern: "useEntries"
    - from: App.tsx
      to: src/entries/EntriesProvider.tsx
      via: "EntriesProvider wraps children inside CategoriesProvider"
      pattern: "EntriesProvider"
---

<objective>
EntriesProvider with uid-scoped onSnapshot + Expenses/Income tabs showing entry lists grouped by date.

Purpose: Proves the data layer works end-to-end — entries flow from Firestore through the provider to the list UI. This is the foundation that the entry form (03-02) and delete/offline features (03-03) build on.
Output: Working entry lists on both tabs with empty states and FAB buttons.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-entries/03-CONTEXT.md
@.planning/phases/03-entries/03-UI-SPEC.md

@src/firebase/queries.ts
@src/categories/CategoriesProvider.tsx
@src/auth/AuthProvider.tsx
@src/theme/tokens.ts
@src/lib/money.ts
@src/lib/dates.ts
@src/screens/CategoriesScreen.tsx
@src/screens/MainTabs.tsx
@src/screens/ExpensesScreen.tsx
@src/screens/IncomeScreen.tsx
@App.tsx
</context>

<tasks>

<task type="tracer">
  <name>EntriesProvider + ExpensesScreen entry list — end-to-end data flow</name>
  <files>src/entries/EntriesProvider.tsx, src/components/EntryRow.tsx, src/components/DateSectionHeader.tsx, src/screens/ExpensesScreen.tsx, App.tsx</files>

  <action>
Create the entries data layer and wire it to the Expenses tab as a vertical slice.

1. Create `src/entries/EntriesProvider.tsx` following the CategoriesProvider pattern exactly (module-level createContext(null), custom hook with null guard, useEffect with cleanup). The provider subscribes to `onSnapshot(entriesByType(uid, "expense"), ...)` and `onSnapshot(entriesByType(uid, "income"), ...)` when `user.uid` is available. Map each doc to `{ id: d.id, uid, type, amount, categoryId, date, description, createdAt, hasPendingWrites: d.metadata.hasPendingWrites }`. Expose `entries: Entry[]`, `addEntry`, `updateEntry`, `deleteEntry`, `copyEntry`, `isLoading: boolean`. For now, `addEntry`/`updateEntry`/`deleteEntry`/`copyEntry` can be thin wrappers calling Firestore `addDoc`/`updateDoc`/`deleteDoc` with the entry collection — full error handling comes in 03-03. Re-subscribe when `user` changes (same cleanup pattern as CategoriesProvider). Unsubscribe on sign-out (set entries to []).

2. Create `src/components/EntryRow.tsx` — a single entry row with `flexDirection: 'row'`, `minHeight: 44`. Left side: category name (body 16px, textPrimary, numberOfLines=1) + date+description (label 14px, textSecondary, numberOfLines=1). Right side: amount (44px weight 700, tabular-nums, color = income or expense based on entry.type). 1px bottom border separator (#E5E7EB). Import `formatCents` from `src/lib/money.ts` for amount display. Import `useCategories` to resolve categoryId to category name. Style everything from `src/theme/tokens.ts`.

3. Create `src/components/DateSectionHeader.tsx` — View with `paddingHorizontal: spacing.md`, `paddingVertical: spacing.sm`, `backgroundColor: colors.background`. Text: label 14px weight 700, textSecondary, accessibilityRole="header". Logic: if date is today → "Today", if yesterday → "Yesterday", else → formatted as "Aug 7" (month abbreviated, day number). Use `today()` and `addDays(-1)` from `src/lib/dates.ts` for comparison.

4. Replace `src/screens/ExpensesScreen.tsx` placeholder with a real screen. Use FlatList (not SectionList — entries are flat, grouped by date via section headers). Group entries by date string using a Map. Render EntryRow for each item. Render DateSectionHeader as ListHeaderComponent for each section. Empty state: centered "No entries yet" (textSecondary, body) + "Tap the + button below to log your first expense." (textSecondary, label). FAB (+) button: position absolute bottom-right (bottom: 24, right: 24), 56x56 circle, colors.accent background, white "+" 28px weight 700. FAB onPress is a no-op for now (navigation wiring in 03-02).

5. Modify `App.tsx`: Add `import { EntriesProvider } from './src/entries/EntriesProvider'` and wrap `<CategoriesProvider>` children with `<EntriesProvider>`. The hierarchy becomes: AuthProvider > EntriesProvider > CategoriesProvider > RootNavigator. EntriesProvider must be inside AuthProvider (needs user.uid) and outside CategoriesProvider (categories depend on entries for usage counts — but that's already handled, so order between Entries and Categories doesn't matter as long as both are inside AuthProvider).
  </action>

  <read_first>
    - src/categories/CategoriesProvider.tsx (pattern to follow exactly for createContext/useContext/useEffect)
    - src/auth/AuthProvider.tsx (provider pattern reference)
    - src/firebase/queries.ts (entriesByType query builder)
    - src/lib/money.ts (formatCents for amount display)
    - src/lib/dates.ts (today, addDays, toDateString for date section headers)
    - src/screens/ExpensesScreen.tsx (current placeholder to replace)
    - src/theme/tokens.ts (all style values)
    - App.tsx (provider hierarchy to modify)
  </read_first>

  <acceptance_criteria>
  - `src/entries/EntriesProvider.tsx` exports `EntriesProvider` component and `useEntries` hook
  - `useEntries()` returns `{ entries, addEntry, updateEntry, deleteEntry, copyEntry, isLoading }`
  - `Entry.type` is `'expense' | 'income'`, `Entry.amount` is number (integer cents)
  - EntriesProvider subscribes to `onSnapshot(entriesByType(uid, type))` for both expense and income
  - EntriesProvider unsubscribes when user signs out (entries becomes [])
  - `src/components/EntryRow.tsx` renders category name, amount with ₱ prefix, date+description
  - `src/components/DateSectionHeader.tsx` renders "Today" / "Yesterday" / "Mon DD" labels
  - `src/screens/ExpensesScreen.tsx` shows FlatList of expense entries with date section headers
  - Empty state shows "No entries yet" and "Tap the + button below to log your first expense."
  - FAB (+) button visible at bottom-right on ExpensesScreen
  - `App.tsx` has EntriesProvider wrapping inside AuthProvider
  - All style values come from `src/theme/tokens.ts` (no hardcoded colors/spacing)
  - `npx expo start` compiles without errors
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open the app on a device with test entries in Firestore — Expenses tab shows entries grouped by date with correct amounts and category names. Empty state appears when no expense entries exist.</manual>
  </verify>

  <done>
    EntriesProvider is wired into the app, ExpensesScreen shows real entries from Firestore grouped by date, empty state renders correctly, FAB is visible. The data layer is proven end-to-end.
  </done>
</task>

<task type="auto">
  <name>IncomeScreen + shared entry components</name>
  <files>src/screens/IncomeScreen.tsx</files>

  <action>
Replace the IncomeScreen placeholder with a real screen mirroring ExpensesScreen.

1. Replace `src/screens/IncomeScreen.tsx` with the same FlatList pattern as ExpensesScreen, but filtering `entries` to `type === 'income'`. Use the same EntryRow, DateSectionHeader, and empty state components. Empty state copy: "No entries yet" + "Tap the + button below to log your first income." FAB (+) button identical to ExpensesScreen.

2. Verify both screens render independently — switching between Expenses and Income tabs shows the correct entry type for each.
  </action>

  <read_first>
    - src/screens/ExpensesScreen.tsx (just created — mirror this pattern)
    - src/entries/EntriesProvider.tsx (useEntries hook interface)
    - src/theme/tokens.ts (style values)
  </read_first>

  <acceptance_criteria>
  - `src/screens/IncomeScreen.tsx` shows FlatList of income entries with date section headers
  - Empty state shows "No entries yet" and "Tap the + button below to log your first income."
  - FAB (+) button visible at bottom-right on IncomeScreen
  - Switching between Expenses and Income tabs shows correct entry types
  - No TypeScript errors
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Switch between Expenses and Income tabs — each shows only its entry type. Empty states render with correct tab-specific copy.</manual>
  </verify>

  <done>
    Both tabs show their respective entries with correct empty states and FAB buttons. The entry list UI is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → EntriesProvider | Entry data crosses from untrusted network to app state; metadata.hasPendingWrites used for sync indicators |
| User input → EntryForm | Amount, category, description cross from user to Firestore; validated before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | EntriesProvider | low | accept | Firestore security rules enforce uid scoping; queries carry explicit uid filter per NFR-01 |
| T-03-02 | Tampering | Entry amount input | low | accept | Amount stored as integer cents; parsePesoInput validates format before conversion |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors
- ExpensesScreen and IncomeScreen render entry lists from Firestore
- Empty states display correctly for both tabs
- EntriesProvider re-subscribes on user change and cleans up on sign-out
</verification>

<success_criteria>
- EntriesProvider is wired into the app hierarchy
- Both tab screens show real entries from Firestore
- Entry rows display category name, formatted amount, and date+description
- Empty states are tab-specific
- FAB buttons are visible on both tabs
</success_criteria>

<output>
Create `.planning/phases/03-entries/03-01-SUMMARY.md` when done
</output>

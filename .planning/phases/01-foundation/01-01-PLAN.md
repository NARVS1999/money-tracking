---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.json
  - tsconfig.json
  - index.ts
  - App.tsx
  - eslint.config.js
  - src/firebase/config.ts
  - src/firebase/app.ts
  - src/theme/tokens.ts
  - src/__tests__/smoke-test.ts
autonomous: false
requirements: [AUTH-02, NFR-05]
user_setup:
  - service: firebase
    why: "Auth + Firestore backend. Real credentials are needed before device sign-in; code is placeholder-safe until then."
    env_vars:
      - name: "firebaseConfig (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId)"
        source: "Firebase console -> Project settings -> Your apps -> Web app (register one if none exists)"
must_haves:
  truths:
    - "App boots end-to-end: `npx expo export --platform android` produces a bundle with no errors (firebase 12 ESM + AsyncStorage 2.2.0 resolve in Metro)"
    - "NFR-05: package.json pins expo SDK 57, firebase ^12.17.1, @react-native-async-storage/async-storage 2.2.0 (via `npx expo install`); `npx expo-doctor` clean"
    - "AUTH-02 wiring: src/firebase/app.ts calls initializeAuth with getReactNativePersistence(AsyncStorage) as the first auth call, at module load"
    - "Firestore initialized with the default memory cache only — no IndexedDB-only durable cache option is passed (CONTEXT: Firebase Init & Persistence; STACK.md Critical Finding)"
    - "Theme tokens exist in src/theme/tokens.ts exactly per UI-SPEC Implementation Contract (income/expense declared but unused in Phase 1)"
    - "jest-expo Wave 0 infra in place: package.json jest preset, tsconfig types includes jest, smoke test passes"
  artifacts:
    - package.json, app.json (name: Money), tsconfig.json, index.ts, eslint.config.js
    - src/firebase/config.ts (placeholder-safe firebaseConfig)
    - src/firebase/app.ts (module-load singletons: app, auth, db)
    - App.tsx (auth-gate skeleton driven by onAuthStateChanged)
    - src/theme/tokens.ts
    - src/__tests__/smoke-test.ts
  key_links:
    - "src/firebase/app.ts -> src/firebase/config.ts (singletons consume config at module load)"
    - "App.tsx -> src/firebase/app.ts auth singleton (gate subscribes via onAuthStateChanged; never calls the default auth getter itself)"
    - "app.json name 'Money' matches UI-SPEC Sign In copy contract"
  prohibitions:
    - "Never pass an IndexedDB-only durable cache option to initializeFirestore in Expo Go (throws; session-scoped memory cache is the accepted MVP scope per STATE.md)"
    - "Never call the default auth getter before initializeAuth runs (that instance lacks AsyncStorage persistence -> AUTH-02 silently fails)"
    - "Never call initializeAuth/initializeFirestore inside components or effects (Fast Refresh re-runs them)"
    - "Never log passwords or token values"
    - "No expo-file-system code this phase; legacy method imports from the main module throw in SDK 57 — record the legacy-import rule for Phase 5 in the SUMMARY"

---

<objective>
**As a** user, **I want to** open the app on my phone and have it boot into a signed-out state behind a secure, persistent auth gate **so that** every later feature (categories, entries, summary, export) builds on a proven Expo SDK 57 + Firebase foundation.

Purpose: This is the tracer plan of Phase 1 — the thinnest production-quality slice through every layer the phase touches: scaffold -> dependency pins -> Firebase module-load singletons (auth persistence + memory-cache Firestore) -> app entry -> auth-gate render. Catching a bundling or persistence-wiring dead end here costs one commit instead of ten.

Output: A bootable Expo SDK 57 app (name "Money") with Firebase JS SDK ^12 wired as a module-load singleton (AsyncStorage 2.2.0 auth persistence per AUTH-02, default memory-cache Firestore), design tokens, jest-expo test infra, and a real firebaseConfig (after the checkpoint decision).

MVP story sourced from ROADMAP Goal line ("User can sign in with email/password and reach an empty, uid-scoped app shell...").
</objective>

## Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | App boots; auth gate skeleton; secure foundation | Task 1 (tracer) |
| SKELETON.md | Walking skeleton — app boots in Expo Go → auth gate → Sign In renders | Task 1 (tracer) implements it |
| REQ | AUTH-02 (persistence wiring), NFR-05 (Expo Go compat, pinned versions) | Task 1 + Task 3 |
| RESEARCH | Pattern 1 singletons; install order; Pitfalls 1-3; Assumptions A1-A6; OQ1 config values | Task 1, Task 2 (checkpoint), Task 3 |
| CONTEXT | Scaffold & Tooling (blank-typescript, src/, config constants, phase gates); Firebase Init & Persistence (singleton, initializeAuth first, memory cache, legacy import rule); theme tokens | Task 1 + Task 3 |
| Deferred | Create-account UI (P6), offline indicator (P3), OFFL-01, SEAR-01 | Excluded (out of scope) |

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/SKELETON.md
</context>

## Artifacts This Phase Produces

- `package.json` — expo ~57, firebase ^12.17.1, async-storage 2.2.0, react-navigation v7 set, jest-expo preset, scripts (start/test/lint)
- `app.json` — name "Money"; template's `userInterfaceStyle: "light"` + `predictiveBackGestureEnabled: false` kept
- `tsconfig.json` — template strict + `"types": ["jest"]`
- `index.ts` — template entry `registerRootComponent(App)` (untouched)
- `App.tsx` — auth-gate skeleton: loading indicator / signed-out view / signed-in view driven by `onAuthStateChanged`
- `src/firebase/config.ts` — placeholder-safe `firebaseConfig` constant (shape: apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId)
- `src/firebase/app.ts` — module-load singletons `app`, `auth` (initializeAuth + AsyncStorage persistence), `db` (memory cache)
- `src/theme/tokens.ts` — colors/spacing/typography/radius per UI-SPEC Implementation Contract
- `src/__tests__/smoke-test.ts` — jest-expo smoke test (App module loads; tokens shape)
- `eslint.config.js` — generated by first `npx expo lint` run

<tasks>

<task type="tracer">
  <name>Task 1: Boot the app end-to-end — scaffold, Firebase singletons, auth-gate skeleton</name>
  <read_first>
    01-RESEARCH.md lines 83-87 (template facts), 140-163 (install commands), 258-278 + 401-415 (singleton pattern), 324-332 (anti-patterns); 01-PATTERNS.md "Scaffold/config files" and "src/firebase/app.ts" sections; 01-UI-SPEC.md Implementation Contract (tokens module, lines 223-241).
  </read_first>
  <files>
    package.json, app.json, tsconfig.json, index.ts, App.tsx, src/firebase/config.ts, src/firebase/app.ts, src/theme/tokens.ts
  </files>
  <action>
    1. Scaffold at repo root with the MANDATORY flags: `npx create-expo-app@latest . --template blank-typescript --no-agents-md --yes` (the --no-agents-md flag is critical — the scaffold would otherwise overwrite the repo's project AGENTS.md; per RESEARCH Pitfall 1). Verify AGENTS.md content is unchanged afterward. If the CLI balks at the non-empty directory despite --yes, scaffold into a temp dir and copy the generated files in (Assumption A1 fallback).
    2. Install deps in exactly this order (RESEARCH lines 142-161):
       - `npx expo install firebase @react-native-async-storage/async-storage` (this pins async-storage 2.2.0 — the Expo Go SDK 57 bundled build; do NOT install it from npm directly, npm latest 3.1.1 breaks in Expo Go per Pitfall 2)
       - `npm install @react-navigation/native @react-navigation/native-stack @react-navigation/bottom-tabs` (pure JS, v7)
       - `npx expo install react-native-screens react-native-safe-area-context` (native peers via expo install only)
    3. `app.json`: change only `name` to "Money". Keep the template's `userInterfaceStyle: "light"` and `predictiveBackGestureEnabled: false` (the react-navigation v7 Android requirement) exactly as generated.
    4. Create `src/firebase/config.ts`: export a `firebaseConfig` constant with the exact web-app field shape — apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId — each holding the literal string "PLACEHOLDER" for now (placeholder-safe per RESEARCH OQ1; real values arrive in Task 3).
    5. Create `src/firebase/app.ts` — the load-bearing file of the phase. Copy the singleton pattern verbatim from RESEARCH lines 401-415: import initializeApp from firebase/app, initializeAuth + getReactNativePersistence from firebase/auth, AsyncStorage from @react-native-async-storage/async-storage, initializeFirestore from firebase/firestore, firebaseConfig from ./config. Module top level: initializeApp(firebaseConfig) -> initializeAuth(app, { persistence: getReactNativePersistence(AsyncStorage) }) -> initializeFirestore(app) with NO cache options (default memory cache — an IndexedDB-only durable cache is prohibited in Expo Go per STACK.md Critical Finding). Export singletons named `app`, `auth`, `db`. Guard rules: this must be the FIRST auth call (never the default auth getter first — that instance lacks persistence and AUTH-02 silently fails, Pitfall 3); never inside components or effects (Fast Refresh re-runs them).
    6. Create `App.tsx` — the gate skeleton that proves the wiring end-to-end. Subscribe to `onAuthStateChanged(auth, ...)` in a useEffect with proper unsubscribe cleanup; local state `initializing` (true until the first emission) and `user`. Render: initializing -> a centered ActivityIndicator (size large, color from tokens); signed-out -> a plain View with the text "Signed out" (placeholder leaf only — 01-02 replaces it with the full SignInScreen); signed-in -> a plain View with the text "Signed in" (placeholder — 01-02 replaces it with MainTabs). The gate structure and firebase wiring are production code; only these two leaf views are placeholders, replaced without architectural change. Do not add navigation packages usage yet (01-02 wires NavigationContainer).
    7. Create `src/theme/tokens.ts` — copy the token module verbatim from UI-SPEC Implementation Contract lines 227-240: colors (background #F7F7F8, surface #FFFFFF, textPrimary #1A1A1A, textSecondary #6B7280, border #E5E7EB, income #16A34A, expense #DC2626, accent #111827, danger #DC2626), spacing (xs 4, sm 8, md 16, lg 24, xl 32, 2xl 48, 3xl 64), typography (display 28/700/34, heading 20/700/24, body 16/400/24, label 14/400/20), radius (sm 8). income/expense are declared but MUST NOT be used this phase (no money surfaces). No style values outside this file.
    8. Commit as `feat(01-01): bootstrap Expo SDK 57 app with Firebase singletons and auth gate`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx expo export --platform android --output-dir .expo-check && npx expo-doctor</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with strict template settings
    - `npx expo export --platform android` bundles with no errors (proves firebase 12 ESM + AsyncStorage 2.2.0 resolve through Metro end-to-end)
    - `npx expo-doctor` reports no version/compat issues
    - Repo AGENTS.md byte-identical before/after scaffold (checked via git diff)
    - package.json shows async-storage at exactly 2.2.0 and firebase ^12.17.1
  </acceptance_criteria>
  <done>
    The app boots end-to-end: scaffold committed, firebase singletons wired at module load with AsyncStorage persistence (AUTH-02 wiring) and default memory-cache Firestore, tokens present, gate renders. Tracer slice is production-quality and committed.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Firebase project credentials — create/provide the real project or approve placeholder deferral</name>
  <read_first>
    01-RESEARCH.md Open Question 1 (lines 578-581) and Assumptions A2, A6; 01-PATTERNS.md "src/firebase/config.ts" section.
  </read_first>
  <decision>
    Provide the real Firebase project configuration for `src/firebase/config.ts` now, or defer with placeholders.
  </decision>
  <context>
    This is a one-way-door decision (REVERSIBILITY_GATES): the projectId is globally unique and the auth users + Firestore data live in this project for the app's lifetime. The code is placeholder-safe — every non-config module is built and unit-tested against "PLACEHOLDER" strings (RESEARCH OQ1 ordering recommendation). Device verification of AUTH-01/02/03 (plans 01-02 device checklist) is blocked until real credentials are in place. The Firebase project itself must be created by the human in the console (no CLI is installed; console admin privilege is also what makes default-account seeding legal later, Assumption A2).
  </context>
  <options>
    <option id="option-a">
      <name>Provide real config now</name>
      <pros>Device verification can happen in the same wave; no re-planning when credentials arrive</pros>
      <cons>Human must do console setup now (create project, enable Email/Password, create Firestore, register web app, paste 6 values)</cons>
    </option>
    <option id="option-b">
      <name>Defer — keep placeholders</name>
      <pros>Zero human time now; all code + unit tests proceed untouched</pros>
      <cons>On-device sign-in checks cannot run until later; AUTH-01/02/03 remain unverified on device</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (paste the 6 firebaseConfig values), or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply real Firebase config (per decision) + jest-expo Wave 0 test infra</name>
  <precondition>Task 2 checkpoint resolved: real firebaseConfig values provided, or placeholder deferral explicitly approved by the human.</precondition>
  <read_first>
    01-RESEARCH.md lines 152-161 (jest install + lint + doctor), 610-660 (Validation Architecture: jest preset, tsconfig types, Wave 0); 01-PATTERNS.md "Unit tests (Wave 0, jest-expo)" section; 01-VALIDATION.md Test Infrastructure table.
  </read_first>
  <files>
    src/firebase/config.ts, package.json, tsconfig.json, eslint.config.js, src/__tests__/smoke-test.ts
  </files>
  <action>
    1. If Task 2 chose option-a: replace the six "PLACEHOLDER" strings in `src/firebase/config.ts` with the exact values the human supplied (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId). Keep the exact field shape; do not rename fields (later phases and the firebase init depend on the shape). If option-b was chosen: leave placeholders untouched and record the deferral in the SUMMARY (device checks blocked until resolved).
    2. Install the Wave 0 test framework (Windows needs the "--" separator before --dev): `npx expo install jest-expo jest @types/jest "--" --dev`. Keep the typescript version at the template pin (~6.0.3) — do not upgrade it manually.
    3. `package.json`: add `"jest": { "preset": "jest-expo" }`, a `"test": "jest"` script, and a `"lint": "expo lint"` script. If importing the AsyncStorage native module errors under jest, add the official mock mapping to the jest config: moduleNameMapper mapping @react-native-async-storage/async-storage to its jest mock module path (documented official pattern; only add if the smoke test fails without it).
    4. `tsconfig.json`: add "jest" to the `types` array (template keeps expo/tsconfig.base + strict).
    5. Run `npx expo lint` once — it generates `eslint.config.js` (flat config, eslint-config-expo) automatically; do not hand-write it.
    6. Create `src/__tests__/smoke-test.ts`: import the App component module (exercises the whole graph including the src/firebase/app.ts module-load init under jest-expo mocks) and the tokens module; assert App is a function and that the tokens colors.background equals #F7F7F8 (locks the token contract). No render — component testing is optional this phase and react-test-renderer is deprecated on React 19.
    7. Commit as `test(01-01): add jest-expo Wave 0 infra and smoke test` (plus the config change if option-a).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --ci --silent && npx expo-doctor</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx jest --ci --silent` passes (smoke test green); `npx expo-doctor` clean
    - package.json contains the jest-expo preset and jest dev deps; tsconfig types includes jest; eslint.config.js exists (generated, not hand-written)
    - config.ts holds real values from option-a, or an approved deferral is recorded in the SUMMARY (placeholders intact)
    - No 3 consecutive tasks without automated verification (VALIDATION sign-off §2)
  </acceptance_criteria>
  <done>
    Wave 0 test infra is live and green; the Firebase config is either real (option-a) or the deferral is documented (option-b). Plan 01-01 complete: app boots, firebase wired, tokens and test harness ready for plans 01-02/01-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device -> Firebase Auth | Email/password credentials cross this boundary at sign-in |
| device -> Firebase config | Public Firebase API key/config constants shipped in the JS bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Firestore queries (future plans) | high | mitigate | uid equality filter on every query in `queries.ts` (01-03) + uid-keyed security rules (01-03); this plan only wires the db singleton — no data access yet |
| T-1-02 | Information Disclosure | Sign-in error handling | medium | mitigate | uniform copy "Email or password is wrong" mapped in `errors.ts` (01-02) — no account-enumeration leak |
| T-1-03 | Elevation of Privilege | Client-side auth checks | high | mitigate | gate in App.tsx is display-only; Firestore rules (01-03) are the enforcement point; no client-side privilege logic exists |
| T-1-04 | Information Disclosure | Logging / config | medium | mitigate | never log passwords or token values (Task 1 discipline); Firebase config constants in `config.ts` are acceptable per the Firebase model (API keys are public by design; protection is auth + rules) |
| T-1-SC | Tampering | npm/expo installs | high | mitigate | Package Legitimacy Audit (RESEARCH) approved all 9 packages — SUS verdicts were recency-only false positives with no postinstall scripts; no blocking human checkpoint required per audit disposition; async-storage pinned exactly 2.2.0 via `npx expo install` |
</threat_model>

<verification>
Full phase gate after this plan: `npx jest --ci --silent && npx tsc --noEmit && npx expo lint && npx expo-doctor` all green.

Device check (manual, requires Task 2 option-a + Expo Go on the phone): `npx expo start` -> scan QR -> app boots to the signed-out gate without redbox (bundling proven on-device, NFR-05).
</verification>

<success_criteria>
- Expo SDK 57 app scaffolds at repo root; repo AGENTS.md survives byte-identical
- Firebase JS SDK ^12 initializes as module-load singletons; AsyncStorage 2.2.0 auth persistence wired before any other auth call (AUTH-02 wiring); Firestore on default memory cache
- `npx expo export --platform android`, `npx tsc --noEmit`, `npx jest --ci --silent`, `npx expo-doctor` all green
- Theme tokens module matches UI-SPEC verbatim; jest-expo preset live with a passing smoke test
- Real firebaseConfig inserted (or deferral approved and recorded)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>


---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/money.ts
  - src/lib/__tests__/money-test.ts
  - src/lib/dates.ts
  - src/lib/__tests__/dates-test.ts
  - src/firebase/queries.ts
  - src/firebase/__tests__/queries-test.ts
  - deploy/firestore.rules
  - deploy/composite-index.md
  - .planning/phases/01-foundation/COVERAGE.md
autonomous: false
requirements: [NFR-01, NFR-03, NFR-04, NFR-06]
user_setup:
  - service: firebase-console
    why: "One-time admin operations the agent cannot perform (no firebase CLI installed): deploy security rules, create the composite index, seed the default account. Gated by Task 3 checkpoint."
    dashboard_config:
      - task: "Publish deploy/firestore.rules verbatim (or with the approved hardening line)"
        location: "Firebase console -> Firestore Database -> Rules"
      - task: "Create composite index entries: type ASC, date DESC"
        location: "Firebase console -> Firestore Database -> Indexes"
      - task: "Seed default account: auth user + users/{uid} doc with isDefault: true (admin privilege bypasses rules — required)"
        location: "Firebase console -> Authentication + Firestore Database"
must_haves:
  truths:
    - "NFR-03: money.ts formats and parses integer cents only — zero float math, zero device-dependent formatter APIs; unit-tested (covered)"
    - "NFR-04: dates.ts produces and consumes local 'YYYY-MM-DD' strings only — no UTC-based slicing; unit-tested (covered)"
    - "NFR-01: every exported query builder starts with a where('uid','==',uid) equality filter; unit-tested via query constraint inspection (covered)"
    - "entriesByType additionally constrains type and orders by date desc — documented against the composite index artifact"
    - "deploy/firestore.rules matches backend-schema.md verbatim (plus the Task 3 hardening decision); deploy/composite-index.md documents the console steps"
    - "COVERAGE.md exists in the phase dir with all 10 API-surface rows and correct INTEGRATE/OPT-OUT dispositions"
    - "deployed rules reject cross-account access — verified with a second test account on device (backstop: requires console deployment + device)"
  artifacts:
    - src/lib/money.ts, src/lib/dates.ts + __tests__/money-test.ts, __tests__/dates-test.ts
    - src/firebase/queries.ts + __tests__/queries-test.ts
    - deploy/firestore.rules, deploy/composite-index.md
    - .planning/phases/01-foundation/COVERAGE.md
  key_links:
    - "src/firebase/queries.ts -> src/firebase/app.ts db singleton (imports db, never re-initializes Firestore)"
    - "entriesByType builder <-> deploy/composite-index.md (type ASC, date DESC is a server-side dependency; the query error self-links if missing)"
    - "deploy/firestore.rules <-> backend-schema.md rules (deploy verbatim — rules are the enforcement point, NFR-06)"
    - "COVERAGE.md <-> every INTEGRATE row maps to a real file/symbol (AuthProvider, app.ts, queries.ts, deploy artifacts)"
  prohibitions:
    - "Never store or compute money as floats — integer cents only; parsing via string math, never float multiplication"
    - "Never slice dates via UTC — local calendar components only (getFullYear/getMonth/getDate)"
    - "No query builder without a uid equality filter — every builder starts with where('uid','==',uid) (rules are not filters)"
    - "Never construct Firestore queries outside queries.ts in any later phase"
    - "No unscoped query anywhere — a missing uid clause fails wholesale with permission-denied once a second account exists"

---

<objective>
**As a** developer building Phases 2-6, **I want to** have deterministic money/date utilities, uid-scoped query builders, and deployed security rules **so that** every later feature stores integer cents, local dates, and private ledger data correctly and securely from day one.

Purpose: This plan hardens the data layer: pure, unit-tested utilities (NFR-03/NFR-04) that later entry/summary/export phases depend on; the centralized uid-scoped query surface (NFR-01) that makes cross-account access impossible to write accidentally; and the deployable security artifacts (rules + composite index, NFR-06) that make it impossible at the server. The API coverage matrix (COVERAGE.md) records what of the Firebase JS SDK surface Phase 1 integrates vs defers.

Output: money.ts, dates.ts, queries.ts with green test suites; deploy/firestore.rules + deploy/composite-index.md console artifacts; COVERAGE.md.
</objective>

## Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Money/date foundations; uid-scoped queries; secure ledger (rules) | Tasks 1-3 |
| REQ | NFR-01 (uid filter on every query), NFR-03 (integer cents, money.js), NFR-04 (local YYYY-MM-DD), NFR-06 (rules deployed matching backend-schema.md) | Tasks 1-3 |
| RESEARCH | Pattern 3/4; Code Examples money/dates/queries/rules; Pitfalls 5-8; OQ2 (amountCents int hardening), OQ3 (query-internals testing); Security Domain rows | Tasks 1-3 |
| CONTEXT | Firebase Init & Persistence (queries centralized under src/firebase/queries.ts); specifics: rules match backend-schema.md, composite index deployment artifact | Tasks 2-3 |
| backend-schema.md | Rules text (lines 68-91) + indexes table (lines 108-114) | Task 2 + Task 3 |
| Deferred | Durable offline (OFFL-01), search (SEAR-01), write paths (Phase 3) | Excluded (out of scope) |

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@backend-schema.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

# 01-01 provides: src/firebase/app.ts db singleton, jest-expo infra, placeholder-safe config. 01-02 runs in the same wave and shares no files with this plan.
</context>

## Artifacts This Phase Produces

- `src/lib/money.ts` — `formatCents(cents: number): string`, `parsePesoInput(input: string): number | null` (integer cents; no Intl, no floats)
- `src/lib/__tests__/money-test.ts` — NFR-03 suite
- `src/lib/dates.ts` — `toDateString`, `today`, `isValid`, `addDays`, `compare`, `monthRange` (local YYYY-MM-DD)
- `src/lib/__tests__/dates-test.ts` — NFR-04 suite
- `src/firebase/queries.ts` — `userDoc`, `entriesBase`, `entriesByType`, `entriesInRange`, `categoryInUse`, `categoriesOf` (all uid-scoped)
- `src/firebase/__tests__/queries-test.ts` — NFR-01 suite
- `deploy/firestore.rules` — console-paste-ready rules (backend-schema.md verbatim + optional hardening)
- `deploy/composite-index.md` — console steps for `entries: type ASC, date DESC`
- `.planning/phases/01-foundation/COVERAGE.md` — Firebase JS SDK API coverage matrix

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: money.ts + dates.ts — deterministic integer-cents and local-date utilities</name>
  <read_first>
    01-RESEARCH.md Code Examples "money.ts (NFR-03...)" (lines 484-501) and "dates.ts (NFR-04...)" (lines 503-528); Pattern 4 (319-322); Pitfalls 5-6 (373-383); 01-PATTERNS.md "src/lib/money.ts" and "src/lib/dates.ts" sections; 01-VALIDATION.md Wave 0 requirements rows for money/dates.
  </read_first>
  <files>
    src/lib/money.ts, src/lib/__tests__/money-test.ts, src/lib/dates.ts, src/lib/__tests__/dates-test.ts
  </files>
  <behavior>
    money.ts:
    - formatCents(0) -> "₱ 0.00"; formatCents(123456) -> "₱ 1,234.56" (space after symbol, separators always); formatCents(5) -> "₱ 0.05"; formatCents(-2450) -> "-₱ 24.50"; formatCents(999999999) -> "₱ 9,999,999.99"
    - parsePesoInput("24.5") -> 2450; parsePesoInput("₱ 1,234.56") -> 123456; parsePesoInput("12") -> 1200; parsePesoInput("0.05") -> 5; parsePesoInput("0.001") -> null (3 decimals rejected); parsePesoInput("1.2.3") -> null; parsePesoInput("abc") -> null; parsePesoInput("") -> null
    dates.ts:
    - toDateString of a local Date (e.g., new Date(2026, 7, 6)) -> "2026-08-06" — LOCAL components, never UTC-derived
    - isValid("2026-02-28") -> true; isValid("2026-02-30") -> false; isValid("2026-13-01") -> false; isValid("2026-1-1") -> false (format); isValid("garbage") -> false
    - addDays("2026-01-31", 1) -> "2026-02-01"; addDays("2026-02-28", 1) -> "2026-03-01" (non-leap); addDays("2024-02-28", 1) -> "2024-02-29" (leap); addDays("2026-08-06", -6) -> "2026-07-31"
    - monthRange("2024-02") -> { start: "2024-02-01", end: "2024-02-29" }; monthRange("2026-12") -> { start: "2026-12-01", end: "2026-12-31" }
    - compare("2026-01-01", "2026-01-02") -> -1; compare("2026-01-01", "2026-01-01") -> 0; compare("2026-02-01", "2026-01-01") -> 1 (lexicographic string compare)
    - today() matches toDateString(new Date()) — near-midnight safe by construction (no UTC path exists in the module)
  </behavior>
  <action>
    RED: create `src/lib/__tests__/money-test.ts` and `src/lib/__tests__/dates-test.ts` covering every case in the behavior block. Run `npx jest --ci --silent src/lib` — MUST fail (modules missing). Commit `test(01-03): add failing money/date utility tests`.
    GREEN: implement `src/lib/money.ts` per the RESEARCH code example: `formatCents` uses integer division via Math.floor on absolute cents, regex thousands grouping, padded fraction; `parsePesoInput` strips symbol/commas/spaces via regex, validates /^\d+(\.\d{0,2})?$/, and derives cents via string split + padEnd — string math, never float arithmetic (RESEARCH Pitfall 6: device-dependent formatter APIs are banned; float drift via division is banned). Implement `src/lib/dates.ts` per the RESEARCH code example: toDateString from getFullYear/getMonth/getDate padded — local calendar math, never UTC-based string slicing (RESEARCH Pitfall 5: UTC+8 midnight bug); isValid via format regex + round-trip Date construction (rejects 2026-02-30); addDays via new Date(y, m-1, d+n); monthRange via new Date(y, m, 0); compare via lexicographic string comparison. Both modules dependency-free and pure. Run the suites — MUST pass. Commit `feat(01-03): implement money and date utilities`.
    REFACTOR (if needed): keep both modules pure and dependency-free; re-run suites green.
  </action>
  <verify>
    <automated>npx jest --ci --silent src/lib && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - money-test.ts and dates-test.ts cover all behavior cases and are green
    - money.ts contains no float math and no device-dependent formatter API usage (NFR-03); dates.ts contains no UTC-derived output (NFR-04)
    - Both modules are dependency-free pure functions (no imports other than types)
  </acceptance_criteria>
  <done>
    NFR-03 and NFR-04 are unit-tested and green; Phase 3/4/5 consumers have their deterministic foundations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: uid-scoped query builders + rules/index deployment artifacts + COVERAGE.md</name>
  <read_first>
    01-RESEARCH.md Pattern 3 (lines 305-317) + Code Examples "uid-scoped query builders" (lines 466-482); Pitfall 7 (rules are not filters, 385-389) + Pitfall 8 (composite index, 391-395); OQ3 (query-internals assertion approach, lines 587-589); backend-schema.md Security rules (lines 64-91) + Indexes (108-114); 01-PATTERNS.md "src/firebase/queries.ts" and "Firestore security rules + composite index" sections; .planning/phases/01-foundation/COVERAGE.md (written at planning; refresh if reality differs).
  </read_first>
  <files>
    src/firebase/queries.ts, src/firebase/__tests__/queries-test.ts, deploy/firestore.rules, deploy/composite-index.md, .planning/phases/01-foundation/COVERAGE.md
  </files>
  <behavior>
    - Every exported QUERY builder (entriesBase, entriesByType, entriesInRange, categoryInUse, categoriesOf) produces a query whose constraint list contains a uid equality filter on field path ["uid"] with the passed uid value (NFR-01). userDoc returns a DocumentReference (doc users/{uid}) and is exempt from the uid-filter assertion — it is not a query.
    - entriesByType(uid, "expense") additionally constrains type == "expense" and orders by date desc
    - entriesInRange(uid, "2026-08-01", "2026-08-31") constrains date >= "2026-08-01" and date <= "2026-08-31"
    - categoryInUse(uid, "cat1") constrains categoryId == "cat1" and applies limit(1)
    - queries.ts imports db from ./app and never calls initializeFirestore itself
  </behavior>
  <action>
    RED: create `src/firebase/__tests__/queries-test.ts` — import the builders and assert the uid filter by inspecting the query's constraint internals (RESEARCH OQ3: read the constraint list from the query object; in firebase 12 the internal structure lives under the query's `_query` property with a filters array — each filter exposes its field path and comparison). Write a local helper `hasUidFilter(q, uid)` and assert it for every builder listed in the behavior block, plus the per-builder constraint checks. If the internal field shape differs in the installed firebase version, adapt the helper to whatever constraint representation the SDK exposes (the assertion target is: a uid equality constraint exists — never assert on serialized SQL-like strings). Run `npx jest --ci --silent src/firebase` — MUST fail. Commit `test(01-03): add failing uid-scoped query tests`.
    GREEN: implement `src/firebase/queries.ts` per the RESEARCH code example: `userDoc(uid)` -> doc(db, "users", uid); `entriesBase(uid)` -> query(collection(db, "entries"), where("uid", "==", uid)); `entriesByType(uid, type)` -> query(entriesBase(uid), where("type", "==", type), orderBy("date", "desc")) — this is the query that requires the composite index; `entriesInRange(uid, start, end)` -> query(entriesBase(uid), where("date", ">=", start), where("date", "<=", end)); `categoryInUse(uid, categoryId)` -> query(entriesBase(uid), where("categoryId", "==", categoryId), limit(1)); `categoriesOf(uid, kind)` -> query(collection(db, kind), where("uid", "==", uid)) with kind typed "expenseCategories" | "incomeCategories". Every builder starts from a uid equality filter (NFR-01) — the uid clause is non-negotiable and first. Run the suite — MUST pass. Commit `feat(01-03): implement uid-scoped query builders`.
    Deployment artifacts (same task, after GREEN):
    - Create `deploy/firestore.rules` — copy the rules text VERBATIM from backend-schema.md lines 68-91 (match /users/{uid}, /entries/{id}, /expenseCategories/{id}, /incomeCategories/{id} exactly as written). Do not alter semantics; the only allowed variation is the Task 3 hardening line, which is appended later if the human approves it.
    - Create `deploy/composite-index.md` — document the console steps for the composite index `entries: type ASC, date DESC` (Firebase console -> Firestore Database -> Indexes -> Add index), state that the entriesByType query error self-links to creation if missing (Pitfall 8), and reference backend-schema.md Indexes table.
    - Refresh `.planning/phases/01-foundation/COVERAGE.md` against the implemented surface: verify every INTEGRATE row maps to a real file/symbol (email/password sign-in -> AuthProvider.tsx; session restore/persistence -> app.ts initializeAuth + AsyncStorage; uid-scoped queries -> queries.ts; rules deployment -> deploy/firestore.rules; composite index -> deploy/composite-index.md) and every OPT-OUT disposition is still accurate (create account/sign out/delete user -> Phase 6; Firestore writes -> Phase 3; offline cache -> memory-cache only per OFFL-01). Edit only if a row's disposition no longer matches reality; otherwise leave as planned.
    Commit `feat(01-03): add firestore rules and index deployment artifacts`.
  </action>
  <verify>
    <automated>npx jest --ci --silent src/firebase && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - queries-test.ts is green: uid equality filter asserted on all 5 query builders, per-builder constraints asserted, userDoc exempt
    - queries.ts imports db from ./app; no Firestore initialization call inside queries.ts
    - deploy/firestore.rules is byte-faithful to backend-schema.md rules (modulo the approved Task 3 hardening line)
    - deploy/composite-index.md documents the type ASC, date DESC index with console steps
    - COVERAGE.md present with all 10 rows; INTEGRATE rows map to existing files; dispositions match the phase plan set
  </acceptance_criteria>
  <done>
    NFR-01 is unit-tested and green; the rules/index artifacts are console-ready; COVERAGE.md records the API surface — every later phase writes queries only through this module.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve one-way console deployment — rules (+ hardening), composite index, default-account seed</name>
  <read_first>
    backend-schema.md Security rules + Default account lifecycle (lines 22, 64-93); 01-RESEARCH.md OQ2 (amountCents is int hardening, lines 583-585), Assumption A2 (console admin bypasses rules — required for seeding), NFR-06 row (line 58); deploy/firestore.rules + deploy/composite-index.md (created in Task 2).
  </read_first>
  <decision>
    Deploy the Firestore security rules and composite index, and seed the default account in the Firebase console — now or deferred? (One-way door per REVERSIBILITY_GATES: rules become the enforcement point for the production database; the default account is created once.)
  </decision>
  <context>
    These are admin-side console operations (no firebase CLI installed): publishing deploy/firestore.rules makes uid scoping + isDefault immutability server-enforced (NFR-06); creating the composite index `entries: type ASC, date DESC` unblocks the future tab-list query (the query error self-links until then); seeding the default account (auth user + users/{uid} with isDefault: true) must happen with admin privilege because the rules themselves forbid in-app default creation (Assumption A2 — the console bypasses rules, which the seed requires). Device verification of cross-account isolation (NFR-06 backstop) and the 01-02 device checklist both depend on this deployment.
  </context>
  <options>
    <option id="option-a">
      <name>Deploy verbatim now</name>
      <pros>Server enforcement live immediately; NFR-06 device verification unblocked; zero divergence from backend-schema.md</pros>
      <cons>No float-drift guard on entries create until Phase 3 touches rules again</cons>
    </option>
    <option id="option-b">
      <name>Deploy now with the amountCents hardening line</name>
      <pros>Same as option-a, plus `request.resource.data.amountCents is int` on the entries create rule prevents float drift reaching the DB (RESEARCH OQ2 — recommended, no downside)</pros>
      <cons>One deliberate one-line divergence from backend-schema.md (append the line to deploy/firestore.rules and document it in the SUMMARY + backend-schema note)</cons>
    </option>
    <option id="option-c">
      <name>Defer deployment</name>
      <pros>Zero console time now</pros>
      <cons>NFR-06 unenforced server-side; AUTH-01/02/03 device checks and the cross-account backstop blocked until deployed; Phase 2 listeners would fail on unscoped reads</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Firestore | Every read/write crosses here; rules are the server-side enforcement point |
| Firestore console (admin) | Human console operations bypass rules (IAM admin) — the only legitimate path for seeding isDefault: true |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Firestore queries / rules | high | mitigate | every builder in queries.ts starts with where("uid","==",uid) (NFR-01, unit-tested); rules key every allow on request.auth.uid == resource.data.uid (deploy/firestore.rules, NFR-06); cross-account access is impossible by construction and verified with a second account (backstop) |
| T-1-02 | Information Disclosure | Sign-in errors | medium | mitigate | uniform "Email or password is wrong" mapping lives in 01-02's errors.ts (unit-tested) — this plan adds no error surface |
| T-1-03 | Elevation of Privilege | Rules vs client checks | high | mitigate | rules are the enforcement point — isDefault: true is creatable only with admin privilege (console seed), create rules require isDefault == false, update preserves isDefault (immutability pattern); client code never decides privileges |
| T-1-04 | Information Disclosure | Logging | medium | mitigate | no password/token logging anywhere; queries.ts logs nothing |
| T-1-SC | Tampering | npm/expo installs | high | mitigate | no new packages in this plan; audit posture from 01-01 stands |
</threat_model>

<verification>
Automated full suite: `npx jest --ci --silent && npx tsc --noEmit && npx expo lint` all green after every task commit.

Deployment + device checklist (manual — preconditions: 01-01 Task 2 real config; Task 3 decision executed):
1. Rules in the console match deploy/firestore.rules (option-a verbatim or option-b with the hardening line) — NFR-06
2. Composite index entries: type ASC, date DESC exists in the console — Pitfall 8 pre-empted
3. Default account seeded: auth user exists + users/{uid} doc with isDefault: true — AUTH-01 device sign-in depends on it
4. Cross-account isolation: with a second test account created in the console, sign in as account A and confirm account B's data is unreadable (and vice versa); no unscoped query ever succeeds (rules are not filters) — NFR-06 backstop
</verification>

<success_criteria>
- money-test/dates-test/queries-test suites green; tsc strict passes; no lint errors
- deploy/firestore.rules + deploy/composite-index.md committed and console-deployed (or deployment deferral recorded with reason per Task 3)
- COVERAGE.md matrix accurate for the whole phase
- Backstop NFR-06 verification executed (or blocked-state recorded with the exact missing precondition)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>


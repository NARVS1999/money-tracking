---
phase: 05-export
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/exportPipeline.ts
  - src/screens/ExportScreen.tsx
autonomous: false
requirements: [EXPT-02, EXPT-05]

must_haves:
  truths:
    - "PDF export contains total expense, total income, per-category totals, and entry list in range"
    - "PDF uses A4 landscape page size for entry table readability"
    - "PDF styling uses green for income amounts and red for expense amounts"
    - "PDF filename follows money-tracking-YYYY-MM-DD-to-YYYY-MM-DD.pdf pattern"
    - "Android saves PDF to Downloads via SAF; iOS opens share sheet"
    - "Export success shows filename in toast; export failure shows error with retry"
    - "Loading spinner visible during PDF generation"
    - "expo-print printToFileAsync generates the PDF in cache before file move"
    - "Cache file is cleaned up after successful export"
  artifacts:
    - path: "src/lib/exportPipeline.ts"
      provides: "Fully functional exportPDF with HTML template, expo-print, and file save"
  key_links:
    - from: "src/lib/exportPipeline.ts"
      to: "expo-print"
      via: "printToFileAsync generates PDF from HTML string"
      pattern: "printToFileAsync"
    - from: "src/lib/exportPipeline.ts"
      to: "src/lib/files.ts"
      via: "saveToFile moves PDF from cache to Downloads/share"
      pattern: "saveToFile"
    - from: "src/lib/exportPipeline.ts"
      to: "src/lib/money.ts"
      via: "formatCents for amount display in PDF"
      pattern: "formatCents"
---

<objective>
Purpose: Expand the PDF export pipeline from the tracer stub into a fully functional export with proper HTML template, expo-print integration, and complete file save flow. This plan delivers EXPT-02 (PDF content requirements) and completes EXPT-05 for PDF format.

Output: Working PDF export that generates a styled A4 landscape document with totals, per-category breakdown, and entry list. File saves to Downloads on Android via SAF and opens share sheet on iOS.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export/05-CONTEXT.md
@.planning/phases/05-export/05-UI-SPEC.md
@.planning/phases/05-export/05-RESEARCH.md
@.planning/phases/05-export/05-01-SUMMARY.md
@src/lib/exportPipeline.ts
@src/lib/files.ts
@src/lib/money.ts
@src/lib/dates.ts
@src/theme/tokens.ts
@src/entries/EntriesProvider.tsx
@src/categories/CategoriesProvider.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full PDF pipeline — HTML template + expo-print + file save</name>
  <files>src/lib/exportPipeline.ts, src/lib/__tests__/exportPipeline-test.ts</files>
  <action>
Expand the exportPDF function and refine the HTML template to produce a complete, styled PDF.

**Step 1 — Add PDF-specific tests (RED):**

Add to `src/lib/__tests__/exportPipeline-test.ts`:
- Test `buildPdfHtml` with 3 expense entries and 2 income entries — verify output contains:
  - Title text with date range
  - "Total Expense:" with formatted amount
  - "Total Income:" with formatted amount
  - At least 3 `<tr>` rows in the tbody
  - Per-category totals (group by categoryId, sum amounts)
- Test `buildPdfHtml` with empty entries array — verify output contains "No entries in this range"
- Test `buildPdfHtml` with entries from multiple categories — verify category names appear (look up via categoryMap)

Run tests — new assertions should fail.

**Step 2 — Implement full PDF pipeline (GREEN):**

Replace the stub `exportPDF` in `src/lib/exportPipeline.ts` with the full implementation:

```typescript
export async function exportPDF(
  entries: Entry[],
  expenseCategories: Category[],
  incomeCategories: Category[],
  fromDate: string,
  toDate: string,
): Promise<string> {
  const html = buildPdfHtml(entries, expenseCategories, incomeCategories, fromDate, toDate);
  const Print = await import('expo-print');
  const { uri } = await Print.printToFileAsync({ html });
  const filename = generateFilename(fromDate, toDate, 'pdf');
  const FileSystem = (await import('expo-file-system/legacy')).default;
  const content = await FileSystem.readAsStringAsync(uri);
  await saveToFile(content, filename, 'utf8');
  return filename;
}
```

Refine `buildPdfHtml` to include per-category breakdown:
- After the summary section, add a "Category Breakdown" section
- Group entries by categoryId, sum amounts per category
- Show category name (looked up from categoryMap), total amount, and count
- Sort categories by total amount descending

The HTML template must:
- Use `@page { size: A4 landscape; margin: 1cm; }` for landscape orientation
- Use inline CSS (no external stylesheets — Expo Print doesn't support them)
- Use `font-family: -apple-system, sans-serif` for cross-platform consistency
- Use the app's color tokens: `#DC2626` for expenses, `#16A34A` for incomes
- Format amounts with `formatCents()` from `src/lib/money.ts`

**Step 3 — Run tests to verify (GREEN):**
```bash
npx jest src/lib/__tests__/exportPipeline-test.ts
```

**Step 4 — Verify build:**
```bash
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd C:/Users/Admin/Desktop/project/money-tracking && npx jest src/lib/__tests__/exportPipeline-test.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <read_first>
    src/lib/exportPipeline.ts (existing stub from Plan 05-01 — expand the implementation)
    src/lib/files.ts (saveToFile function — used for final file save)
    src/lib/money.ts (formatCents — for amount formatting in HTML)
    src/lib/dates.ts (monthRange, today — for date formatting in PDF title)
    src/theme/tokens.ts (color values used in HTML — #DC2626, #16A34A, #F7F7F8, #E5E7EB)
    src/entries/EntriesProvider.tsx (Entry type — shape of data for PDF rows)
    src/categories/CategoriesProvider.tsx (Category type — for category name lookup)
  </read_first>
  <behavior>
    - exportPDF generates HTML with title showing date range
    - exportPDF includes total expense and total income with formatted amounts
    - exportPDF includes per-category breakdown with category names and totals
    - exportPDF includes entry list table with date, type, category, amount, description
    - PDF uses A4 landscape page size
    - Amounts are green (#16A34A) for income, red (#DC2626) for expense
    - Empty range shows "No entries in this range" in the PDF
    - expo-print printToFileAsync generates PDF in cache directory
    - File is saved to final destination via saveToFile (SAF on Android, share on iOS)
    - Cache file is cleaned up after export
    - Filename follows money-tracking-YYYY-MM-DD-to-YYYY-MM-DD.pdf pattern
    - All tests pass
    - TypeScript compiles cleanly
  </behavior>
  <done>
    PDF export generates a complete, styled A4 landscape document with totals, per-category breakdown, and entry list. The file is saved to Downloads on Android (SAF) or shared on iOS. All unit tests pass and TypeScript compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device verification — PDF export on phone</name>
  <what-built>
Full PDF export pipeline: ExportScreen → buildPdfHtml → expo-print → saveToFile (SAF/share).
  </what-built>
  <how-to-verify>
1. Run `npx expo start` and scan QR with Expo Go on your phone
2. Navigate to the Export tab
3. Leave the default date range (This Month)
4. Add at least 2-3 entries in the Expenses or Income tabs first (so there's data to export)
5. Tap the "PDF" format button
6. Verify: "Generating…" text appears briefly
7. Verify: Success toast appears with filename like "money-tracking-2026-08-01-to-2026-08-31.pdf"
8. Android: Verify SAF permission dialog appears; grant access; verify file appears in Downloads
9. iOS: Verify share sheet opens; choose a location; verify file is saved
10. Open the PDF and verify:
    - Title shows "Money Tracking — {date range}"
    - Total Expense and Total Income are shown
    - Per-category breakdown is shown
    - Entry list table has Date, Type, Category, Amount, Description columns
    - Amounts are green for income, red for expense
    - Page is landscape oriented
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → expo-print | HTML string passed to printToFileAsync; must be well-formed |
| App → Filesystem | SAF permission request; cache → final destination file move |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Denial of Service | buildPdfHtml | low | accept | Large entry lists slow HTML generation but don't crash; loading spinner covers UX |
| T-05-05 | Information Disclosure | PDF content | low | accept | PDF contains only user's own data; saved to user-controlled location |
| T-05-06 | Tampering | expo-print | low | accept | expo-print is SDK-bundled; HTML is generated from trusted data, not user input |
</threat_model>

<verification>
1. `npx jest src/lib/__tests__/exportPipeline-test.ts` — all tests pass
2. `npx tsc --noEmit` — no errors
3. Device: Export tab shows with date pickers and format buttons
4. Device: PDF export generates a styled A4 landscape document
5. Device: File saves to Downloads (Android) or share sheet (iOS)
6. Device: Success toast shows filename
7. Device: PDF contains totals, category breakdown, and entry list
</verification>

<success_criteria>
PDF export is fully functional: generates a styled document with all required content (totals, category breakdown, entry list), saves to the correct platform destination, and shows success/error feedback. All automated tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-export/05-02-SUMMARY.md` when done
</output>

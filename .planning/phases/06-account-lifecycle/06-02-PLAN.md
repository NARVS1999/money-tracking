---
phase: 06
plan: "06-02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/auth/AuthProvider.tsx
  - src/screens/AccountScreen.tsx
autonomous: true
requirements: [AUTH-05, AUTH-06, AUTH-07]
---

<objective>
Build the full Account tab: display name, email, "Default" badge for the seeded account, sign out button, and a delete-account flow that reauthenticates the user, cascade-deletes all Firestore data in chunked batches (entries → categories → users doc → auth account last), then redirects to Sign In. Default accounts never see the delete option.
</objective>

<tasks>

<task>
<type>execute</type>
<files>src/auth/AuthProvider.tsx</files>
<action>Extend AuthProvider with signOut, reauthenticate, deleteAccount methods, a userProfile state, and an isOnline connectivity flag. All additions go into the same AuthProvider component and AuthContextValue type.

**Imports to add:** signOut (firebase/auth), reauthenticateWithCredential, EmailAuthProvider, deleteUser (firebase/auth), getDocs, query, where, limit, deleteDoc (firebase/firestore), doc (firebase/firestore — already imported), db (firebase/app — already imported).

**userProfile state:** Add useState<{ displayName: string; email: string; isDefault: boolean } | null>(null). In the onAuthStateChanged callback, when a user signs in (u is non-null), fetch the users/{uid} doc via getDoc(doc(db, "users", u.uid)). Extract displayName (string, fallback to ""), email (from user.email, fallback to ""), isDefault (boolean, fallback to false). Set userProfile. When user signs out (u is null), set userProfile to null. Wrap the getDoc in a try/catch — if the doc doesn't exist (legacy account or race), set defaults. Expose userProfile in the context value.

**isOnline state:** Add useState<boolean>(true). Add a useEffect that runs when user is non-null: subscribe to onSnapshot(doc(db, "users", user.uid), { includeMetadataChanges: true }, (snap) => setIsOnline(!snap.metadata.fromCache)). Return the unsubscribe in cleanup. When user is null, set isOnline to true (assume online when signed out). Expose isOnline in context.

**signOut method:** useCallback wrapping signOut(auth). No return value needed — onAuthStateChanged clears user and userProfile automatically. Expose in context.

**reauthenticate method:** useCallback(async (password: string) => { if (!auth.currentUser) throw new Error("Not signed in"); const credential = EmailAuthProvider.credential(auth.currentUser.email!, password); await reauthenticateWithCredential(auth, credential); }). Throws on wrong password — caller handles. Expose in context.

**deleteAccount method:** useCallback(async (password: string) => { if (!user) throw new Error("Not signed in"); Step 1 — reauthenticate: call reauthenticate(password) (above). Step 2 — cascade delete entries: loop while true, query entriesBase(user.uid) with limit(500), if empty break, deleteDoc each, await all. Step 3 — cascade delete expenseCategories: same pattern with collection "expenseCategories", where("uid","==",user.uid), limit(500). Step 4 — cascade delete incomeCategories: same with "incomeCategories". Step 5 — delete users/{uid} doc: await deleteDoc(doc(db, "users", user.uid)). Step 6 — delete auth account LAST: await deleteUser(auth.currentUser). }). The cascade is idempotent — if app crashes mid-deletion, re-running picks up remaining docs. Each batch uses getDocs (not onSnapshot) for a point-in-time snapshot. All Firestore operations are sequential within a batch (no parallel deletes — simpler error handling). Expose deleteAccount in context.}). The complete AuthContextValue type should include: user, initializing, signIn, signUp (from 06-01), signOut, userProfile, isOnline, reauthenticate, deleteAccount.</action>
<verify>App.tsx compiles; AuthProvider exposes signOut, reauthenticate, deleteAccount, userProfile, isOnline; signOut clears user state; deleteAccount cascade deletes entries then categories then users doc then auth account</verify>
<acceptance_criteria>AuthProvider has signOut (calls firebase signOut), reauthenticate (EmailAuthProvider.credential + reauthenticateWithCredential), deleteAccount (reauthenticate → chunked entries → chunked categories → users doc → deleteUser), userProfile ({ displayName, email, isDefault }), isOnline flag</acceptance_criteria>
<read_first>src/auth/AuthProvider.tsx, src/firebase/app.ts, src/firebase/queries.ts, src/entries/EntriesProvider.tsx, src/categories/CategoriesProvider.tsx</read_first>
</task>

<task>
<type>execute</type>
<files>src/screens/AccountScreen.tsx</files>
<action>Replace the PlaceholderScreen import with a full AccountScreen implementation. Use useAuth() to get user, userProfile, signOut, deleteAccount, isOnline.

**Layout:** Scrollable SafeAreaView (or View with flex:1) with background colors.background. A card (same style as SignInScreen: surface background, border, radius.sm, padding lg) containing:

1. **Header section:** userProfile.displayName as heading (typography.heading, colors.textPrimary). Below it, userProfile.email as secondary text (typography.body, colors.textSecondary). If userProfile.isDefault, show a "Default" badge next to or below the display name — a small View with backgroundColor: colors.accent, borderRadius: radius.sm, paddingHorizontal: spacing.sm, paddingVertical: spacing.xs, containing a Text with "Default" in typography.label, color: colors.onAccent.

2. **Sign out button:** Full-width button (same style as SignInScreen's button — colors.accent background, onAccent label). Label: "Sign out". On press: call signOut() from AuthProvider. No confirmation needed — immediate sign out, auth gate swaps to SignInScreen.

3. **Delete account section:** Only rendered when !userProfile.isDefault (AUTH-05: default account never shows delete). A "Delete account" button with colors.danger background and colors.onAccent label. Below it, helper text in typography.label, colors.textSecondary: "This will permanently delete all your data." On press: open a delete confirmation modal.

**Delete confirmation modal:** A React Native Modal (visible state controlled by a boolean). Inside the modal: a card-style overlay with:
- Heading: "Delete account" (typography.heading)
- Copy: "Type your password to confirm. This cannot be undone." (typography.body, colors.textSecondary)
- Password TextInput (secureTextEntry, same input style as SignInScreen)
- Error display below password (same error style)
- "Cancel" button (secondary style — border, no fill) that closes the modal and clears password state
- "Delete" button (colors.danger background) — disabled while submitting or when password is empty. In-flight label: "Deleting..."
- On press Delete: call deleteAccount(password) from AuthProvider. On success: do nothing — the auth gate reacts to onAuthStateChanged and swaps to SignInScreen (the user is signed out after deleteUser). On error: display error inline in the modal (wrong password → "Password is incorrect"; network error → "Check your connection and try again"; other → generic message).
- When modal opens, password state is cleared.

**Offline handling:** The delete button should have reduced opacity and be disabled when !isOnline (AUTH-06: disabled when offline). Add a small helper text below the delete button when offline: "Delete unavailable offline" in typography.label, colors.textSecondary.

**Loading state:** While userProfile is null (still fetching), show a centered ActivityIndicator on the background token (same pattern as LoadingScreen). Once userProfile loads, show the full UI. If userProfile is null after loading (shouldn't happen in normal flow), show a fallback message.

**Styles:** Reuse SignInScreen's StyleSheet patterns — same card, same input, same button, same error styles. Add new styles for: badge (flexDirection row, alignItems center), modal overlay (flex 1, backgroundColor 'rgba(0,0,0,0.4)', justifyContent center, alignItems center), modal card (same as screen card but with max width), deleteSection (marginTop xl, borderTop with colors.border, paddingTop lg).</action>
<verify>AccountScreen renders with display name, email, and sign out button; default account shows "Default" badge and no delete button; non-default account shows delete button; tapping sign out returns to SignInScreen; tapping delete opens modal; entering wrong password shows error; entering correct password deletes account and returns to SignInScreen</verify>
<acceptance_criteria>AccountScreen shows display name, email, Default badge (if isDefault), sign out button, delete button (non-default only); delete flow: modal → password → reauthenticate → cascade → SignIn; delete button disabled when offline; default account never shows delete option</acceptance_criteria>
<read_first>src/screens/AccountScreen.tsx, src/screens/PlaceholderScreen.tsx, src/screens/SignInScreen.tsx, src/auth/AuthProvider.tsx, src/theme/tokens.ts</read_first>
</task>

</tasks>

<verification>
1. Sign in as the default account → Account tab shows display name, email, "Default" badge, sign out button, NO delete button
2. Sign in as a non-default account → Account tab shows display name, email, sign out button, delete button with helper text
3. Tap "Sign out" → immediately lands on SignInScreen; session is cleared
4. Tap "Delete account" → modal appears asking for password
5. Enter wrong password → error "Password is incorrect" displayed in modal
6. Enter correct password → cascade runs (entries deleted, categories deleted, users doc deleted, auth account deleted) → lands on SignInScreen
7. Try to sign in with the deleted account's email → "Email or password is wrong" (account no longer exists)
8. Create a new account (06-01) → sign in → Account tab shows new account with no badge, delete available
9. Delete the new account → cascade works → back to SignInScreen
10. Verify with 600+ entries: delete completes, no dangling docs (check Firestore console)
</verification>

<success_criteria>
- AUTH-05: Default account shows "Default" badge and never offers delete option
- AUTH-06: Non-default account can be deleted: reauthenticate → all data gone → back to Sign In
- AUTH-06: Cascade is chunked (≤500 docs/batch) and idempotent
- AUTH-06: deleteUser is called LAST (after all Firestore docs are deleted)
- AUTH-07: Sign out from Account tab returns to Sign In screen
- AccountScreen displays real data (displayName, email) from users/{uid} doc
- Delete button is disabled when offline
</success_criteria>

<must_haves>
- truths:
  - AccountScreen displays the user's display name and email from the users/{uid} Firestore doc
  - Default account (isDefault: true) shows a "Default" badge pill
  - Default account does NOT render a delete button
  - Non-default account renders a delete button with helper text
  - Delete button is disabled when isOnline is false
  - Delete flow opens a modal requiring password input
  - Reauthentication uses EmailAuthProvider.credential with the entered password
  - Cascade deletes entries in chunks of ≤500 docs, then expenseCategories, then incomeCategories
  - users/{uid} doc is deleted after all subcollections are empty
  - deleteUser(auth) is called LAST — only after Firestore docs are gone
  - After deleteUser, onAuthStateChanged fires and auth gate swaps to SignInScreen
  - Sign out calls signOut(auth) and auth gate swaps to SignInScreen immediately
  - userProfile state is fetched from users/{uid} on sign-in and cleared on sign-out
- artifacts:
  - src/auth/AuthProvider.tsx (extended with signOut, reauthenticate, deleteAccount, userProfile, isOnline)
  - src/screens/AccountScreen.tsx (replaced placeholder with full implementation)
- key_links:
  - AccountScreen.useAuth() → userProfile for display name, email, isDefault
  - AccountScreen signOut → AuthProvider.signOut → signOut(auth) → onAuthStateChanged → auth gate
  - AccountScreen delete → modal → reauthenticate → deleteAccount → cascade → deleteUser → onAuthStateChanged → auth gate
  - AuthProvider.deleteAccount → reauthenticateWithCredential → getDocs/deleteDoc loop → deleteDoc users/{uid} → deleteUser
  - AuthProvider.isOnline → onSnapshot users/{uid} with includeMetadataChanges → !fromCache
</must_haves>

---
phase: 06
plan: "06-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/AuthProvider.tsx
  - src/auth/errors.ts
  - src/screens/SignUpScreen.tsx
  - src/screens/SignInScreen.tsx
  - App.tsx
autonomous: true
requirements: [AUTH-04]
---

<objective>
Extend the auth system with in-app account creation: a signUp method in AuthProvider that creates a Firebase user and writes a users/{uid} document, a SignUpScreen with display name/email/password fields, and a "Create account" link on SignInScreen that navigates to the new screen. After sign-up the new account has an empty ledger and the user is signed in immediately.
</objective>

<tasks>

<task>
<type>execute</type>
<files>src/auth/AuthProvider.tsx, src/auth/errors.ts</files>
<action>Extend AuthProvider with a signUp(displayName, email, password) method. Import createUserWithEmailAndPassword from @firebase/auth and setDoc, doc, Timestamp from firebase/firestore, plus db from ../firebase/app. Add signUp to the AuthContextValue type. Implementation: call createUserWithEmailAndPassword(auth, email.trim(), password) to get the UserCredential, then await setDoc(doc(db, "users", userCredential.user.uid), { displayName: displayName.trim(), email: email.trim().toLowerCase(), isDefault: false, createdAt: Timestamp.now() }). The user is already signed in after createUserWithEmailAndPassword (no second signIn call needed). On error, let it propagate — the caller maps it via a sign-up-specific error function. In errors.ts, add a signUpErrorMessage function that handles auth/email-already-in-use ("An account with this email already exists"), auth/weak-password ("Password must be at least 6 characters"), auth/invalid-email ("Invalid email address"), and falls back to the same default copy as authErrorMessage. Export signUpErrorMessage alongside authErrorMessage. Do NOT change authErrorMessage — existing sign-in callers must not break.</action>
<verify>App.tsx compiles without errors; AuthProvider exports signUp in its context value; signUpErrorMessage handles the three sign-up-specific codes</verify>
<acceptance_criteria>AuthProvider.signUp creates a Firebase user and writes users/{uid} doc with isDefault:false; signUpErrorMessage maps all three sign-up error codes to user-friendly copy</acceptance_criteria>
<read_first>src/auth/AuthProvider.tsx, src/auth/errors.ts, src/firebase/app.ts, src/firebase/queries.ts</read_first>
</task>

<task>
<type>execute</type>
<files>src/screens/SignUpScreen.tsx</files>
<action>Create a new SignUpScreen matching SignInScreen's visual design exactly (same card, same input styles, same button, same error placement). Three fields: Display Name (TextInput, autoCapitalize="words", textContentType="name", autoComplete="name"), Email (TextInput, keyboardType="email-address", autoCapitalize="none", autoCorrect={false}, textContentType="username", autoComplete="email"), Password (TextInput, secureTextEntry, textContentType="newPassword", autoComplete="new-password"). Button label "Create account" — disabled when any field is empty, when password is under 6 characters, or while submitting. In-flight label: "Creating account...". On press: call signUp(displayName, email, password) from useAuth(). On success: do nothing — the auth gate in App.tsx reacts to onAuthStateChanged and swaps to MainTabs. On error: display the error string from signUpErrorMessage below the Password field (same error Text style as SignInScreen). Password field returnKeyType="go" and onSubmitEditing triggers submit. Display Name field returnKeyType="next" and onSubmitEditing focuses Email. Email field onSubmitEditing focuses Password. Use the same KeyboardAvoidingView, Platform check, and StyleSheet patterns from SignInScreen. Import colors, radius, spacing, typography from ../theme/tokens and useAuth from ../auth/AuthProvider and signUpErrorMessage from ../auth/errors. The screen should accept navigation prop via useNavigation hook for a potential "Already have an account? Sign in" link (but this link is wired in Task 3, not here).</action>
<verify>npx expo start compiles; SignUpScreen renders with three fields and a disabled button; entering valid data enables the button; tapping with short password shows "Password must be at least 6 characters"</verify>
<acceptance_criteria>SignUpScreen has Display Name, Email, Password fields; button is disabled when fields empty or password under 6 chars; errors display inline; success triggers auth gate swap</acceptance_criteria>
<read_first>src/screens/SignInScreen.tsx, src/theme/tokens.ts, src/auth/AuthProvider.tsx</read_first>
</task>

<task>
<type>execute</type>
<files>src/screens/SignInScreen.tsx, App.tsx</files>
<action>In SignInScreen, add a "Create account" text link below the Sign in button. Use useNavigation() from @react-navigation/native to get the navigation prop. The link is a Pressable with a Text child styled as secondary text (typography.body, colors.textSecondary) that calls navigation.navigate("SignUp") on press. Add marginTop spacing.md above the link to separate it from the button. In App.tsx RootNavigator, add the SignUp screen to the signedOut conditional branch: import SignUpScreen from "./src/screens/SignUpScreen" and add Stack.Screen name="SignUp" component={SignUpScreen} alongside the existing SignIn screen. The signedOut branch becomes two screens: SignIn and SignUp. Both are headerless (headerShown: false is inherited from screenOptions).</action>
<verify>Tapping "Create account" on SignInScreen navigates to SignUpScreen; pressing back (or "Already have an account?" if added) returns to SignInScreen; the full flow (tap Create account → fill fields → submit → lands on MainTabs) works end-to-end</verify>
<acceptance_criteria>SignInScreen has a "Create account" link that navigates to SignUpScreen; App.tsx registers SignUp in the signedOut stack; end-to-end sign-up flow works: create account → signed in → empty ledger</acceptance_criteria>
<read_first>src/screens/SignInScreen.tsx, App.tsx, src/screens/SignUpScreen.tsx</read_first>
</task>

</tasks>

<verification>
1. Open the app — lands on SignInScreen (no sign-up on first run, per AUTH-01)
2. Tap "Create account" — navigates to SignUpScreen with three empty fields and a disabled button
3. Enter display name, email, password (under 6 chars) — button stays disabled; error "Password must be at least 6 characters" appears when password field is touched and submitted
4. Enter valid password (6+ chars) — button enables
5. Submit with an email that already exists — error "An account with this email already exists" appears inline
6. Submit with a new email — account is created, user is signed in, lands on MainTabs with an empty ledger
7. Sign out (if 06-02 is done) — can sign back in with the newly created account
</verification>

<success_criteria>
- AUTH-04: User can create an additional account in-app (display name, email, password min 6 chars) which starts with an empty ledger and signs into the new account immediately
- AuthProvider.signUp exists and is callable
- SignUpScreen renders with all three fields
- SignInScreen has "Create account" link
- App.tsx registers SignUp in the signedOut stack
- Error handling covers email-already-in-use, weak-password, invalid-email
</success_criteria>

<must_haves>
- truths:
  - SignUpScreen has Display Name, Email, and Password fields
  - Password validation enforces minimum 6 characters client-side
  - createUserWithEmailAndPassword is called with the provided credentials
  - A users/{uid} doc is written with { displayName, email, isDefault: false, createdAt }
  - After sign-up the auth gate swaps to MainTabs (user is signed in)
  - The new account's ledger is empty (no entries, no categories exist for this uid)
  - SignInScreen has a "Create account" link that navigates to SignUpScreen
  - App.tsx signedOut stack includes both SignIn and SignUp screens
- artifacts:
  - src/screens/SignUpScreen.tsx (new file)
  - src/auth/AuthProvider.tsx (extended with signUp)
  - src/auth/errors.ts (extended with signUpErrorMessage)
  - src/screens/SignInScreen.tsx (extended with link)
  - App.tsx (extended with SignUp screen)
- key_links:
  - AuthProvider.signUp → createUserWithEmailAndPassword → setDoc users/{uid}
  - SignInScreen "Create account" → navigation.navigate("SignUp") → SignUpScreen
  - SignUpScreen submit → AuthProvider.signUp → onAuthStateChanged → auth gate swap
</must_haves>

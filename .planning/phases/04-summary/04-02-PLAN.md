---
phase: 04-summary
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/EmptyState.tsx
  - src/screens/HomeScreen.tsx
autonomous: false
requirements:
  - SUMM-03

must_haves:
  truths:
    - "Empty state shows 'Nothing logged this month' centered heading when no entries exist for current month"
    - "Empty state shows 'Start tracking to see your summary here.' body text"
    - "Empty state shows 'Add an entry' CTA button with colors.accent background and white text"
    - "CTA button calls onAddPress when pressed (no-op for MVP — navigation deferred)"
    - "Summary updates immediately when app is opened in a new month — entries from new month appear, previous month excluded"
    - "Summary updates immediately when entry is added, edited, or deleted — no manual refresh needed"
    - "Month header updates to new month at month boundary"
  artifacts:
    - path: src/components/EmptyState.tsx
      provides: "Empty state component with heading, body text, and CTA button"
    - path: src/screens/HomeScreen.tsx
      provides: "Home screen with empty state integration"
  key_links:
    - from: src/screens/HomeScreen.tsx
      to: src/components/EmptyState.tsx
      via: "Conditional rendering when monthEntries.length === 0"
      pattern: "EmptyState"
    - from: src/screens/HomeScreen.tsx
      to: src/lib/dates.ts
      via: "monthRange(today()) for month-boundary correctness"
      pattern: "monthRange"
---

<objective>
Empty state + add CTA, month-boundary correctness, and live update verification.

Purpose: Handles the edge cases the tracer (04-01) left open — what the user sees when no entries exist, and verification that the summary correctly handles month boundaries and live updates. The empty state is a natural part of the summary experience.
Output: Working empty state with CTA button, verified month-boundary behavior, verified live updates.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-summary/04-CONTEXT.md
@.planning/phases/04-summary/04-UI-SPEC.md

@src/screens/HomeScreen.tsx
@src/entries/EntriesProvider.tsx
@src/categories/CategoriesProvider.tsx
@src/lib/dates.ts
@src/lib/money.ts
@src/theme/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>EmptyState component with CTA button</name>
  <files>src/components/EmptyState.tsx, src/screens/HomeScreen.tsx</files>

  <action>
Create an EmptyState component and integrate it into HomeScreen for the empty month case.

1. Create `src/components/EmptyState.tsx` — View container centered both vertically and horizontally (`flex: 1`, `justifyContent: 'center'`, `alignItems: 'center'`, `paddingHorizontal: spacing.xl`). Props: `{ onAddPress: () => void }`. Three children stacked vertically with `gap: spacing.md`:
   - Heading Text: "Nothing logged this month" — heading style (20px, weight '700', textPrimary), `textAlign: 'center'`
   - Body Text: "Start tracking to see your summary here." — label style (14px, weight '400', textSecondary), `textAlign: 'center'`
   - CTA Pressable: "Add an entry" — View with `backgroundColor: colors.accent`, `paddingHorizontal: spacing.lg`, `paddingVertical: spacing.sm`, `borderRadius: radius.sm`, `marginTop: spacing.md`. Text inside: body style (16px, weight '400', color '#FFFFFF'), `textAlign: 'center'`. onPress calls `onAddPress` prop.

2. Modify `src/screens/HomeScreen.tsx` to import and conditionally render EmptyState. After the derivation logic, add: `if (monthEntries.length === 0 && !isLoading) return <EmptyState onAddPress={() => {}} />`. The onAddPress is a no-op for MVP — per CONTEXT.md "Add an entry" CTA navigates to the appropriate tab, but for MVP just show the CTA without navigation. Place this check BEFORE the main summary render but AFTER the loading check.
  </action>

  <read_first>
    - src/screens/HomeScreen.tsx (just created in 04-01 — to add EmptyState)
    - src/theme/tokens.ts (colors, spacing, radius for empty state styling)
    - .planning/phases/04-summary/04-UI-SPEC.md (empty state spec: "Nothing logged this month" + body + CTA)
    - src/screens/PlaceholderScreen.tsx (pattern reference for centered layout)
  </read_first>

  <acceptance_criteria>
  - `src/components/EmptyState.tsx` exports a function component with props `{ onAddPress: () => void }`
  - EmptyState renders "Nothing logged this month" as heading (20px, weight '700', textPrimary)
  - EmptyState renders "Start tracking to see your summary here." as body (14px, textSecondary)
  - EmptyState renders "Add an entry" CTA button
  - CTA button has colors.accent background (#111827) and white text (#FFFFFF)
  - CTA button has borderRadius radius.sm (8px)
  - CTA button calls onAddPress when pressed
  - HomeScreen imports EmptyState
  - HomeScreen shows EmptyState when monthEntries.length === 0 and isLoading is false
  - HomeScreen shows summary when monthEntries.length > 0
  - HomeScreen shows LoadingSkeleton when isLoading is true (empty state and loading are mutually exclusive)
  - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open app with no entries for current month — empty state shows with heading, body, and CTA button. CTA button is tappable (no-op for now).</manual>
  </verify>

  <done>
    Empty state displays correctly when no entries exist for the current month. The CTA button is visible and tappable (no-op). Loading and empty states are mutually exclusive.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Month-boundary correctness and live update verification</name>

  <what-built>
The Home screen summary derives totals and per-category breakdowns from cached entries filtered to the current month via monthRange(today()). Empty state shows when no entries exist. Loading skeleton shows during initial load.
  </what-built>

  <how-to-verify>
1. **Empty state**: Open the app with no entries for the current month. Verify "Nothing logged this month" heading and "Add an entry" CTA button appear centered on the screen.

2. **Populated state**: Add 2-3 expense entries and 1-2 income entries across different categories. Verify:
   - Month header shows current month (e.g. "August 2026")
   - Expense total shows correct sum in red (#DC2626)
   - Income total shows correct sum in green (#16A34A)
   - Expense categories appear in "Expenses" section sorted by amount descending
   - Income categories appear in "Income" section sorted by amount descending
   - Section subtotals match the totals

3. **Live update**: Add a new entry from the Expenses tab, then switch back to Home. Verify the summary updates immediately (expense total increases, new category appears or existing category amount increases).

4. **Month boundary**: If possible, change the phone's date to the next month and reopen the app. Verify the summary resets to empty (no entries for the new month). Change back to the original month — verify entries reappear.

5. **Category resolution**: Verify category names display correctly (not "Unknown") for all categories with entries.
  </how-to-verify>

  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EntriesProvider → HomeScreen | Entry data crosses from context to summary display; empty state is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | Empty state CTA | low | accept | CTA is a no-op for MVP; navigation to entry tabs deferred to implementation discretion |
| T-04-04 | Elevation of Privilege | Month boundary | low | accept | monthRange() uses local date strings (NFR-04); no server-side date manipulation |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors
- Empty state renders with correct copy and CTA when no entries exist
- Month-boundary correctness verified on device
- Live update verified on device — adding/editing/deleting entries updates summary immediately
- Category names resolve correctly (no "Unknown" entries)
</verification>

<success_criteria>
- Empty state displays "Nothing logged this month" + body + "Add an entry" CTA
- Summary updates immediately when entries change
- Month boundary correctly excludes previous month's entries
- All verification steps pass on device
</success_criteria>

<output>
Create `.planning/phases/04-summary/04-02-SUMMARY.md` when done
</output>

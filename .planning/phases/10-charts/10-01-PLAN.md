# Phase 10: Charts - Plan

**Created:** 2026-08-09
**Mode:** mvp
**Requirements:** CHRT-01, CHRT-02, CHRT-03, CHRT-04, CHRT-05, CHRT-06, NFR-07, NFR-10

## Wave 1: Install & Setup

### Task 1: Install react-native-svg
- Run: `npx expo install react-native-svg`
- Verify Expo Go compatibility

## Wave 2: Chart Components

### Task 2: Create chart color palette
- File: `src/components/chartColors.ts` (new)
- Export 8-color palette for chart segments
- Export helper to get color by index

### Task 3: Create DonutChart component
- File: `src/components/DonutChart.tsx` (new)
- Props: `data: { name: string; value: number; color: string }[]`, `size?: number`
- Renders SVG donut with colored arcs
- Center shows total
- Groups slices <5% into "Other"

### Task 4: Create ChartLegend component
- File: `src/components/ChartLegend.tsx` (new)
- Props: `items: { name: string; percent: number; color: string }[]`
- Renders colored dots + category name + percentage

## Wave 3: Home Screen Integration

### Task 5: Add charts to HomeScreen
- File: `src/screens/HomeScreen.tsx`
- Import DonutChart and ChartLegend
- Compute chart data from expenseBreakdown/incomeBreakdown
- Render two chart sections between BudgetCard and CategorySection
- Section titles: "Expenses by Category" and "Income by Category" with uppercase styling

## Wave 4: Verify

### Task 6: Run typecheck + lint
- Run `npx tsc --noEmit`
- Fix any type errors

### Task 7: Visual verification
- Verify donut charts render on Home
- Verify legends show correct percentages
- Verify "Other" grouping for small slices

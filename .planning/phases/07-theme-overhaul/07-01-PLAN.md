# Phase 7: Theme Overhaul - Plan

**Created:** 2026-08-09
**Mode:** mvp
**Requirements:** THEM-01, THEM-02, THEM-03, THEM-04, THEM-05, THEM-06, THEM-07, THEM-08

## Wave 1: Design Tokens + Gradient Card

Update the foundational tokens and create the gradient summary card component.

### Task 1: Update design tokens
- File: `src/theme/tokens.ts`
- Add new colors: `primary: '#EF6D40'`, `primaryDark: '#DB281C'`, `teal: '#45C0CF'`, `yellow: '#F8C519'`
- Update `background` from `'#F7F7F8'` to `'#FAFAFA'`
- Update `accent` from `'#111827'` to `'#EF6D40'` (orange becomes the primary action color)
- Add radius tokens: `md: 16`, `lg: 24`, `icon: 14`
- Add shadow tokens for cards
- Keep backward-compatible: `expense`, `income`, `danger` stay the same

### Task 2: Install expo-linear-gradient
- Run: `npx expo install expo-linear-gradient`
- Verify it's in Expo Go (bundled in SDK 57)

### Task 3: Create SummaryCard component
- File: `src/components/SummaryCard.tsx` (new)
- Props: `balanceCents`, `expenseCents`, `incomeCents`, `monthLabel`
- Orange/red gradient background via `LinearGradient`
- White text, 48px balance display, expense/income stat cards inside
- Drop shadow via `elevation` + `shadowColor`

## Wave 2: Home Screen Rebuild

Rebuild the Home screen with the new gradient card, quick-action buttons, and updated layout.

### Task 4: Rebuild HomeScreen
- File: `src/screens/HomeScreen.tsx`
- Replace `<SummaryTotals>` + balance text with `<SummaryCard>`
- Add quick-action buttons below the card ("+ Expense", "+ Income")
- Update section title styling for chart sections (uppercase small-caps)
- Keep existing data derivation logic unchanged

### Task 5: Update CategorySection
- File: `src/components/CategorySection.tsx`
- Add icon placeholder (initial letter in colored rounded square) to left of each row
- Replace flat rows with rounded card container
- Update header styling to match section-title spec (uppercase, small-caps)

## Wave 3: Tab Bar + Navigation

Update the tab bar with frosted-glass effect and active-state styling.

### Task 6: Update MainTabs
- File: `src/screens/MainTabs.tsx`
- Tab bar: semi-transparent white background, no borderTop
- Active tab: orange background highlight (rounded pill)
- Inactive tab: transparent
- Tab label: 11px, 600 weight
- Tab icons: keep Ionicons, update colors

## Wave 4: Auth + Entry Screens

Update SignIn, SignUp, and EntryForm with new card styling.

### Task 7: Update SignInScreen
- File: `src/screens/SignInScreen.tsx`
- Card: white surface, 24px border radius, shadow elevation
- Title: orange accent color
- Button: orange background (new accent), 16px radius
- Input: 12px radius, subtle border

### Task 8: Update SignUpScreen
- File: `src/screens/SignUpScreen.tsx`
- Same changes as SignInScreen for visual consistency

### Task 9: Update EntryForm
- File: `src/components/EntryForm.tsx`
- Card/modal: white surface, 24px radius
- Input fields: 12px radius
- Buttons: orange accent, 16px radius

## Wave 5: List Screens + Components

Update Expenses, Income, Categories, Export, Account screens and shared components.

### Task 10: Update ExpensesScreen + IncomeScreen
- Files: `src/screens/ExpensesScreen.tsx`, `src/screens/IncomeScreen.tsx`
- FAB: orange accent, 28px radius
- Empty state: updated styling
- Total text: updated to match new typography

### Task 11: Update CategoriesScreen
- File: `src/screens/CategoriesScreen.tsx`
- Section headers: updated styling
- Inline add input: 12px radius
- Category rows: rounded, with icon placeholder space
- Swipe actions: keep existing colors

### Task 12: Update ExportScreen
- File: `src/screens/ExportScreen.tsx`
- Cards/surfaces: white with shadow
- Buttons: orange accent, 16px radius
- Date picker inputs: 12px radius

### Task 13: Update AccountScreen
- File: `src/screens/AccountScreen.tsx`
- Card: white surface, 24px radius, shadow
- Buttons: orange accent (sign out), danger (delete)
- Badge: orange background

### Task 14: Update EntryRow + DateSectionHeader
- Files: `src/components/EntryRow.tsx`, `src/components/DateSectionHeader.tsx`
- EntryRow: rounded card, icon placeholder space
- DateSectionHeader: updated label styling

### Task 15: Update EmptyState + LoadingSkeleton + SyncButton
- Files: `src/components/EmptyState.tsx`, `src/components/LoadingSkeleton.tsx`, `src/components/SyncButton.tsx`
- EmptyState: orange CTA button, updated text colors
- LoadingSkeleton: skeleton rectangles with rounded corners (16px)
- SyncButton: keep functionality, update icon color to textPrimary

## Wave 6: Verify

### Task 16: Run typecheck + lint
- Run `npx tsc --noEmit` and `npx eslint src/`
- Fix any type errors from token changes

### Task 17: Visual verification
- Verify all screens render correctly with new tokens
- Check gradient card renders on both platforms
- Verify tab bar styling

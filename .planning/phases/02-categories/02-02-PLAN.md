---
phase: 02-categories
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/screens/CategoriesScreen.tsx
  - src/screens/__tests__/CategoriesScreen.test.ts
  - App.tsx
  - package.json
autonomous: false
requirements: [CATS-01, CATS-02, CATS-03, CATS-04]
user_setup: []

must_haves:
  truths:
    - "Categories tab shows Expense Categories and Income Categories as two separate, non-collapsible groups with sticky section headers"
    - "Each group has an always-visible inline TextInput (placeholder 'New category') and '+' TouchableOpacity — keyboard return/done AND '+' button both submit"
    - "Submitting a case-insensitive duplicate (trimmed) shows inline error 'Already exists' in danger color below the input; error clears on next onChangeText"
    - "Submitting a blank/whitespace-only input is a silent no-op — no error, no Firestore write"
    - "On successful add: input clears, onSnapshot fires, new row appears in the list — no manual state mutation"
    - "Each category row shows right-aligned usage count: '{N} entries' / '1 entry' for populated, '—' for loading/empty usageMap"
    - "Swiping left on a category row reveals the action panel — native-thread gesture via Swipeable (~2.32.0)"
    - "In-use category (usageMap.get(id) > 0): swipe reveals grey '#E5E7EB' panel with 'In use' text '#6B7280' — NOT tappable, no Alert, no action"
    - "Unused category (count === 0): swipe reveals red '#DC2626' panel with 'Delete' text '#FFFFFF' — tapping triggers Alert.alert('Delete {name}?', 'This cannot be undone.', [Cancel, Delete])"
    - "Delete confirmation: Cancel closes the alert, Delete calls deleteCategory(kind, categoryId) → onSnapshot removes the row"
    - "CategoriesProvider wraps the navigator in App.tsx alongside AuthProvider — mounted above the tab navigator so data is ready when Categories tab mounts"
    - "Empty group renders 'No {expense/income} categories yet' centered in the section body with the inline add input still visible in the sticky header"
    - "Per-group empty states are independent: one group populated shows rows; the other shows empty copy — both headers + inputs always visible"
    - "Category names render single-line with numberOfLines={1}; SectionList handles 0, 1, N categories per group natively"
    - "Firestore write rejection for add surfaces inline ('Already exists' slot reused as 'Couldn't add category. Try again.'); delete rejection surfaces via Alert.alert"
    - "Swipe action width is 80px, row height ≥44px (touch target min), inline add TextInput height 48px, section header height 48px"
    - "Accessibility: section headers have accessibilityRole='header'; inline inputs have per-group accessibilityLabels; '+' buttons disambiguate per group; error Text has accessibilityRole='alert'; category rows include count in accessibilityLabel"
    - "Font scaling enabled everywhere — no allowFontScaling={false}; layouts tolerate 150% scaling"
  artifacts:
    - path: "src/screens/CategoriesScreen.tsx"
      provides: "Full Categories screen — SectionList with two grouped sections, per-group inline add inputs, swipe-to-delete rows with in-use guard, live usage counts, empty states"
      exports: ["default CategoriesScreen"]
    - path: "src/screens/__tests__/CategoriesScreen.test.ts"
      provides: "Component tests for two-section rendering, inline add flow, usage count display, swipe states (in-use vs delete), empty states, accessibility roles"
    - path: "App.tsx"
      provides: "Updated auth gate shell — wraps CategoriesProvider around the navigator alongside AuthProvider"
  key_links:
    - from: "src/screens/CategoriesScreen.tsx"
      to: "src/categories/CategoriesProvider.tsx"
      via: "useCategories() hook — the sole data source for the screen; all state (categories, usageMap, add/delete functions) flows through this hook"
      pattern: "useCategories"
    - from: "src/screens/CategoriesScreen.tsx"
      to: "src/theme/tokens.ts"
      via: "imports colors, spacing, typography, radius — single source for all style values"
      pattern: "colors|spacing|typography|radius"
    - from: "App.tsx"
      to: "src/categories/CategoriesProvider.tsx"
      via: "CategoriesProvider wraps children — mounted between SafeAreaProvider and RootNavigator"
      pattern: "CategoriesProvider"
    - from: "src/screens/CategoriesScreen.tsx"
      to: "react-native-gesture-handler"
      via: "import { Swipeable } from 'react-native-gesture-handler' — the swipe-to-delete gesture component (~2.32.0, Expo Go bundled)"
      pattern: "Swipeable"
    - from: "src/screens/CategoriesScreen.tsx"
      to: "src/screens/MainTabs.tsx"
      via: "MainTabs imports CategoriesScreen as the Categories tab component — already wired in Phase 1, no change needed"
      pattern: "CategoriesScreen"
  prohibitions:
    - "Never import Swipeable from react-native-gesture-handler/Swipeable subpath — use the named export from the main module"
    - "Never use gesture-handler version > ~2.32.0 — npm latest 3.1.0 requires Reanimated and breaks in Expo Go"
    - "Never install react-native-reanimated this phase — not needed for v2.x Swipeable"
    - "Never use a FlatList with manual section headers — use core SectionList (stickySectionHeadersEnabled, renderSectionHeader)"
    - "Never build custom PanResponder + Animated for swipe — use the bundled Swipeable (native-thread, deterministic)"
    - "Never render raw Firestore error objects in the UI — always map to locked copy strings"

---

<objective>
**As a** user, **I want to** see my expense and income categories in two groups, add new ones inline, and swipe-to-delete with safety guards, **so that** my categories screen is fast (sub-3-second data display) and I never accidentally delete a category I'm using.

Purpose: This is the tracer plan of Phase 2 — it proves the full Categories pipeline end-to-end: gesture-handler install → CategoriesProvider → SectionList with sticky headers and inline add → Swipeable rows → live usage counts → App.tsx wiring. The whole screen is production quality, not a prototype.

Output: A fully functional Categories tab replacing the Phase 1 placeholder — `CategoriesScreen.tsx` with SectionList (two grouped sections, sticky headers, per-group inline add, swipe-to-delete rows with in-use guard, live usage counts), `CategoriesScreen.test.ts` component tests, updated `App.tsx` with CategoriesProvider wrapper, and `package.json` with gesture-handler installed.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-categories/02-RESEARCH.md
@.planning/phases/02-categories/02-CONTEXT.md
@.planning/phases/02-categories/02-UI-SPEC.md
@src/auth/AuthProvider.tsx
@src/categories/CategoriesProvider.tsx
@src/firebase/queries.ts
@src/theme/tokens.ts
@src/screens/MainTabs.tsx
@src/screens/PlaceholderScreen.tsx
@src/screens/CategoriesScreen.tsx
@App.tsx
@jest.config.js

<interfaces>
From src/categories/CategoriesProvider.tsx (built by Plan 02-01):
```typescript
export function CategoriesProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useCategories(): {
  expenseCategories: { id: string; name: string; createdAt: Timestamp }[];
  incomeCategories: { id: string; name: string; createdAt: Timestamp }[];
  usageMap: Map<string, number>;
  addCategory: (kind: "expenseCategories" | "incomeCategories", name: string) => Promise<void>;
  deleteCategory: (kind: "expenseCategories" | "incomeCategories", categoryId: string) => Promise<void>;
};
```

From src/theme/tokens.ts:
```typescript
export const colors: { background: '#F7F7F8'; surface: '#FFFFFF'; textPrimary: '#1A1A1A'; textSecondary: '#6B7280'; border: '#E5E7EB'; income: '#16A34A'; expense: '#DC2626'; accent: '#111827'; danger: '#DC2626' };
export const spacing: { xs: 4; sm: 8; md: 16; lg: 24; xl: 32; '2xl': 48; '3xl': 64 };
export const typography: { display: { size: 28; weight: '700'; lineHeight: 34 }; heading: { size: 20; weight: '700'; lineHeight: 24 }; body: { size: 16; weight: '400'; lineHeight: 24 }; label: { size: 14; weight: '400'; lineHeight: 20 } };
export const radius: { sm: 8 };
```

From App.tsx (current Phase 1 structure):
```typescript
export default function App(): JSX.Element;
// Structure: SafeAreaProvider > AuthProvider > StatusBar + RootNavigator
// RootNavigator: LoadingScreen | NavigationContainer > Stack (SignIn | MainTabs)
```

From src/screens/MainTabs.tsx:
```typescript
// 5-tab bottom-tabs navigator: Home, Expenses, Income, Categories, Account
// Categories tab: <Tab.Screen name="Categories" component={CategoriesScreen} />
// Already wired — no change needed.
```
</interfaces>
</context>

## Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | User can manage two category groups with inline add, usage counts, safe delete | Tasks 1 (tracer), 2 (tests), 3 (checkpoint) |
| REQ | CATS-01 — two separate groups displayed | Task 1 (SectionList with two sections) |
| REQ | CATS-02 — inline add to either group | Task 1 (per-group TextInput + '+' button + onSubmitEditing) |
| REQ | CATS-03 — usage count displayed per category row | Task 1 (usageMap.get(item.id) render with singular/plural/'—') |
| REQ | CATS-04 — in-use delete blocked, unused deletes after confirmation | Task 1 (Swipeable renderRightActions: grey 'In use' vs red 'Delete' → Alert.alert) |
| RESEARCH | Pattern 2 (SectionList + sticky headers + inline add), Pattern 3 (Swipeable row), Pitfall 1 (gesture-handler version), Pitfall 4 (Swipeable+scroll) | Task 1 |
| RESEARCH | Code Examples SectionList + Swipeable (lines 488-583) | Task 1 |
| RESEARCH | Installation: `npx expo install react-native-gesture-handler` | Task 1 (Wave 0) |
| CONTEXT | Category Group Layout: stacked, sticky headers, non-collapsible, 44px rows | Task 1 |
| CONTEXT | Inline Add UX: fixed input per group, keyboard return + '+' button, case-insensitive trim duplicate with 'Already exists' | Task 1 |
| CONTEXT | Delete Interaction: swipe left, grey 'In use' for used, red 'Delete' + Alert for unused | Task 1 |
| CONTEXT | Usage Count: right-aligned, singular/plural, live via onSnapshot, '—' while loading | Task 1 |
| CONTEXT | the agent's Discretion: Swipeable library choice → gesture-handler (~2.32.0) per RESEARCH | Task 1 |
| UI-SPEC | All 8 UI Considerations (covered → must_haves.truths) | Tasks 1 + 2 |
| Deferred | None | — |

## Artifacts This Phase Produces

- `src/screens/CategoriesScreen.tsx` — Full Categories screen replacing Phase 1 placeholder; SectionList with two sections (Expense/Income Categories), sticky headers with per-group inline add (TextInput + '+' TouchableOpacity), Swipeable category rows with in-use guard (grey 'In use') and delete action (red 'Delete' → Alert.alert confirmation), right-aligned live usage counts (singular/plural/'—'), per-group empty states
- `src/screens/__tests__/CategoriesScreen.test.ts` — Component tests covering: renders two sections with correct titles, renders category names and usage counts per row, renders empty state per group, inline add submit calls addCategory, duplicate error displays and clears, swipe renders 'In use' when count > 0, swipe renders 'Delete' when count === 0, delete tap triggers Alert.alert, accessibility roles on headers/inputs/errors
- `App.tsx` — Updated: wraps CategoriesProvider inside SafeAreaProvider alongside AuthProvider (before RootNavigator)
- `package.json` — Updated: `react-native-gesture-handler` added at ~2.32.0 (via `npx expo install`)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end Categories screen — install gesture-handler, build CategoriesScreen, wire App.tsx</name>

  <read_first>
    02-RESEARCH.md lines 99-101 (gesture-handler install + verify), lines 140-161 (Anti-Patterns), lines 393-416 (Swipeable Pattern 3 + copy contract), lines 488-583 (SectionList + Swipeable full code example), lines 586-628 (duplicate check code example); 02-CONTEXT.md all locked decisions; 02-UI-SPEC.md full Interaction Contract, State Contract, Color section (swipe action colors), Copywriting Contract, Accessibility section; src/categories/CategoriesProvider.tsx (the useCategories hook shape); src/theme/tokens.ts (all token values used in styles); src/screens/MainTabs.tsx (Categories tab already wired at line 31); App.tsx (current structure to add CategoriesProvider wrapper); src/screens/PlaceholderScreen.tsx (being replaced).
  </read_first>

  <files>
    package.json, src/screens/CategoriesScreen.tsx, App.tsx
  </files>

  <action>
    **Wave 0 — Install gesture-handler:**
    1. Run `npx expo install react-native-gesture-handler` — this pins to ~2.32.0 (Expo Go SDK 57 bundled version). Do NOT run `npm install react-native-gesture-handler` (gets 3.1.0 — breaks without Reanimated installed).
    2. Run `npx expo-doctor` — verify no version compat issues.
    3. Commit this install as a standalone refactor commit before the main feature: `chore(02-02): install react-native-gesture-handler ~2.32.0`

    **Step A — Rewrite CategoriesScreen.tsx:**
    Replace the Phase 1 placeholder wrapper (`export default function CategoriesScreen() { return <PlaceholderScreen />; }`) with the full implementation.

    Imports (exhaustive):
    - `useState` from react
    - `SectionList`, `TextInput`, `TouchableOpacity`, `Text`, `View`, `StyleSheet`, `Alert` from react-native
    - `Swipeable` from `react-native-gesture-handler` — **named export from the main module, NOT from /Swipeable subpath**
    - `colors`, `spacing`, `typography`, `radius` from `../theme/tokens`
    - `useCategories`, `Category`, `CategoryKind` from `../categories/CategoriesProvider`

    Local state (useState):
    - `expenseInput: string` — controlled value for the expense group inline add TextInput
    - `incomeInput: string` — controlled value for the income group inline add TextInput
    - `error: string | null` — unified error slot (one active error at a time, last submit attempt wins)

    Helper: `handleAdd(kind: CategoryKind, input: string, setInput: (s: string) => void)` — async, calls `addCategory(kind, input)`, clears input + error on success, sets `error` to `e.message || "Couldn't add category. Try again."` on failure. Blank/whitespace-only input is a silent return (no error, no Firestore call).

    SectionList configuration:
    - `sections` array with two entries:
      ```typescript
      { title: "Expense Categories", kind: "expenseCategories" as const, data: expenseCategories }
      { title: "Income Categories", kind: "incomeCategories" as const, data: incomeCategories }
      ```
    - `stickySectionHeadersEnabled={true}`
    - `keyExtractor`: `(item) => item.id`
    - `ItemSeparatorComponent`: renders a View with height 1, backgroundColor colors.border

    renderSectionHeader:
    - Container View: backgroundColor colors.background, paddingHorizontal spacing.md, paddingVertical spacing.sm, minHeight 48px
    - Section title Text: `section.title`, typography.heading.size, typography.heading.weight fontWeight, colors.accent, `accessibilityRole="header"`
    - Inline add row (View, flexDirection row, alignItems center):
      - TextInput (flex 1): value from the corresponding state variable (expenseInput for expenseCategories, incomeInput for incomeCategories), `onChangeText` updates the state AND calls `setError(null)` (clears error on keystroke per CONTEXT + UI-SPEC Interaction §5), `placeholder="New category"`, `placeholderTextColor={colors.textSecondary}`, `returnKeyType="done"`, `onSubmitEditing` calls handleAdd with the current input value, height 48px, marginTop spacing.sm, borderWidth 1, borderColor colors.border (idle) or colors.accent (focused — track via local `focusedInput` state or `onFocus`/`onBlur`), borderRadius radius.sm, paddingHorizontal spacing.md, typography.body.size, color colors.textPrimary, `accessibilityLabel="New {expense/income} category"`
      - '+' TouchableOpacity (marginLeft spacing.sm, minWidth 44px, height 48px, justifyContent center, alignItems center): Text "+", typography.body.size, typography.heading.weight fontWeight, colors.accent; `onPress` calls handleAdd with the current input value; `accessibilityLabel="Add {expense/income} category"`
    - Error Text (Conditional — only rendered when `error` is non-null): `accessibilityRole="alert"`, colors.danger, typography.label.size, paddingTop spacing.xs; text content = error string

    renderItem:
    - Wraps each row in `<Swipeable renderRightActions={...}>`
    - `renderRightActions` return: if `usageMap.get(item.id) > 0` → View (width 80, backgroundColor '#E5E7EB', justifyContent center, alignItems center) with Text "In use" (color '#6B7280', typography.label.size) — **no onPress, no touch handler**
    - If count === 0 → TouchableOpacity (width 80, backgroundColor colors.danger, justifyContent center, alignItems center) with Text "Delete" (color '#FFFFFF', typography.label.size). `onPress` calls:
      ```javascript
      Alert.alert(`Delete ${item.name}?`, "This cannot be undone.", [
        { text: "Cancel", style: "cancel" },
        { text: "Delete", style: "destructive", onPress: () => deleteCategory(section.kind, item.id).catch(() => Alert.alert("Error", "Couldn't delete category. Try again.")) }
      ])
      ```
    - Row content View: flexDirection row, justifyContent space-between, alignItems center, paddingHorizontal spacing.md, minHeight 44px, backgroundColor colors.background
    - Category name Text: `item.name`, typography.body.size, color colors.textPrimary, flex 1, `numberOfLines={1}`
    - Usage count Text: `usageMap.size === 0 ? "—" : \`${count} ${count === 1 ? "entry" : "entries"}\``, typography.label.size, color colors.textSecondary, marginLeft spacing.sm
    - `accessibilityLabel`: `${item.name}, ${countLabel}` where countLabel is "—" / "1 entry" / "N entries"

    ListEmptyComponent:
    - Parameterized by `section`: renders centered Text (paddingVertical spacing.xl, textAlign center, typography.body.size, color colors.textSecondary): `"No {section.kind === "expenseCategories" ? "expense" : "income"} categories yet"`

    Style references (from tokens):
    - All spacing uses the spacing scale (sm=8, md=16, etc.)
    - All colors use the colors object (textPrimary, textSecondary, accent, danger, background, border)
    - All font sizes/weights use typography object properties
    - Touch target minimums: rows ≥44px, '+' button ≥44px wide × 48px tall, inputs 48px tall

    **Step B — Wire CategoriesProvider in App.tsx:**
    - Import `CategoriesProvider` from `./src/categories/CategoriesProvider`
    - Wrap CategoriesProvider INSIDE SafeAreaProvider, OUTSIDE RootNavigator — same level as AuthProvider:
      ```typescript
      <SafeAreaProvider>
        <AuthProvider>
          <CategoriesProvider>
            <StatusBar style="dark" />
            <RootNavigator />
          </CategoriesProvider>
        </AuthProvider>
      </SafeAreaProvider>
      ```
    - The provider order matters: CategoriesProvider's useEffect depends on useAuth().user.uid, so AuthProvider must be the outer wrapper.

    **Step C — Verify the full pipeline:**
    - `npx tsc --noEmit` exits 0
    - `npx jest --ci --silent` passes (existing Phase 1 tests + 02-01 provider tests — no screen tests yet)
    - `npx expo export --platform android` bundles successfully (proves gesture-handler resolves in Metro)
    - `npx expo-doctor` reports no issues
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx jest --ci --silent && npx expo export --platform android --output-dir .expo-check && npx expo-doctor</automated>
  </verify>

  <acceptance_criteria>
    - `npx expo install react-native-gesture-handler` completed successfully — package.json shows the dependency at ~2.32.0
    - `npx expo-doctor` clean after install — no version incompatibilities
    - `npx tsc --noEmit` exits 0 — all TypeScript types resolve (CategoriesProvider, Swipeable, SectionList)
    - `npx expo export --platform android` bundles with no Metro errors — proves react-native-gesture-handler ~2.32.0 resolves in the Expo Go build
    - `src/screens/CategoriesScreen.tsx` is no longer the Phase 1 placeholder — imports useCategories, renders SectionList with 2 sections
    - `App.tsx` wraps CategoriesProvider between AuthProvider and RootNavigator — provider mount order is AuthProvider (outer) > CategoriesProvider (inner)
    - Swipeable import uses named export from `react-native-gesture-handler` (NOT the `/Swipeable` subpath)
    - Every style value comes from `src/theme/tokens.ts` — no hardcoded hex values (except the swipe action colors per UI-SPEC Color section: '#E5E7EB' for 'In use' bg, '#6B7280' for 'In use' text)
    - The full Phase 1 jest suite still passes — no regressions
  </acceptance_criteria>

  <done>
    The end-to-end Categories pipeline works: gesture-handler is installed at ~2.32.0, CategoriesScreen renders a SectionList with two grouped sections (Expense/Income Categories), per-group inline add inputs with duplicate error handling, swipe-to-reveal rows with in-use guard (grey 'In use') and delete action (red 'Delete' → Alert.alert), and live usage counts from the provider. CategoriesProvider wraps the navigator in App.tsx. All existing tests pass and the Metro bundle succeeds. Committed as a single atomic tracer commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategoriesScreen component tests + edge-case coverage</name>

  <read_first>
    02-RESEARCH.md Validation Architecture (lines 689-722) — test framework specs, Phase Requirements → Test Map; 02-UI-SPEC.md State Contract (all state → visual mappings); jest.config.js (existing Phase 1 config with @firebase CJS moduleNameMapper and AsyncStorage mock); src/categories/CategoriesProvider.tsx (mock target for useCategories); src/screens/CategoriesScreen.tsx (the implementation from Task 1).
  </read_first>

  <files>
    src/screens/__tests__/CategoriesScreen.test.ts
  </files>

  <behavior>
    Write component tests that prove the CategoriesScreen renders correctly for all state combinations defined in the UI-SPEC State Contract.

    **Mock strategy:**
    - Mock `../categories/CategoriesProvider`'s `useCategories` to return controlled test data — the screen tests assert rendering behavior, not Firestore behavior
    - Mock `react-native-gesture-handler`'s `Swipeable` as a simple View wrapper that renders children + calls renderRightActions — the native gesture behavior is verified on device, not in jest
    - Mock `react-native`'s `Alert.alert` to spy on calls

    **Test cases (minimum):**
    1. **Renders two sections with correct titles** — SectionList shows 'Expense Categories' and 'Income Categories' headers
    2. **Renders category names and usage counts** — with mock expenseCategories [{id:'1', name:'Food'}], incomeCategories [{id:'2', name:'Salary'}], usageMap: new Map([['1', 5], ['2', 0]]) — 'Food' shows '5 entries', 'Salary' shows '0 entries'
    3. **Renders singular usage count** — usageMap with 1 → '1 entry'
    4. **Renders '—' loading state** — usageMap.size === 0 → '—' for all rows
    5. **Renders empty state per group** — expenseCategories: [] → 'No expense categories yet' text visible; inline add input still in header
    6. **Inline add submit calls addCategory** — type in expense input, press '+' → addCategory called with ('expenseCategories', trimmed text); input clears after success
    7. **Duplicate error displays and clears** — addCategory rejects with 'Already exists' → error Text visible with danger color; onChangeText clears the error
    8. **Swipe renders 'In use' when count > 0** — renderRightActions for an item with usageMap count > 0 produces Text 'In use' with grey background
    9. **Swipe renders 'Delete' when count === 0** — renderRightActions for an item with count 0 produces TouchableOpacity with Text 'Delete' on red background
    10. **Delete tap triggers Alert.alert** — press the Delete swipe action → Alert.alert called with title containing the category name, message 'This cannot be undone.', and Cancel/Delete buttons
    11. **Delete confirm calls deleteCategory** — press 'Delete' in Alert → deleteCategory called with correct kind and categoryId
    12. **Accessibility roles present** — section headers have accessibilityRole='header'; error Text has accessibilityRole='alert'; '+' buttons have per-group accessibilityLabels
    13. **Blank input is silent no-op** — submit blank/whitespace-only input → addCategory NOT called, no error displayed
  </behavior>

  <action>
    1. Create `src/screens/__tests__/CategoriesScreen.test.ts`.
    2. Write ALL 13 test cases following the behavior specification above. Tests that require async behavior (addCategory calls) use async/await with jest fake timers if needed.
    3. Run `npx jest src/screens --passWithNoTests` — all tests MUST fail (module not yet implemented, or tests target the wrong mock shape).
    4. If any passing test before implementation changes, fix the test (it's testing mock behavior, not screen behavior).
    5. Run `npx jest src/screens` — all tests MUST pass with the already-implemented CategoriesScreen from Task 1.
    6. Run `npx jest --ci --silent` — full suite still green (no regressions).
    7. Run `npx tsc --noEmit` — exits 0.
    8. Run `npx expo lint` — clean.
    9. Commit: `test(02-02): add CategoriesScreen component tests`
  </action>

  <verify>
    <automated>npx jest src/screens --ci --silent && npx jest --ci --silent</automated>
  </verify>

  <acceptance_criteria>
    - All 13 test cases pass — `npx jest src/screens --ci --silent` green
    - Full jest suite (Phase 1 + Plan 02-01 + Plan 02-02) passes — `npx jest --ci --silent` green
    - `npx tsc --noEmit` exits 0 — test file has no type errors
    - `npx expo lint` clean — no lint warnings in test file
    - No test asserts on implementation details (no `instance.state`, no `wrapper.state`, no React internal methods) — tests assert on rendered output and mock call counts
  </acceptance_criteria>

  <done>
    CategoriesScreen has 13 component tests covering all states: populated groups, empty groups, inline add flow, duplicate error lifecycle, usage counts (singular/plural/loading), swipe in-use vs delete states, delete confirmation flow, and accessibility roles. Full Phase 1 + Phase 2 test suite green. Committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification — Categories tab on Expo Go</name>

  <read_first>
    02-CONTEXT.md all locked decisions; 02-UI-SPEC.md Interaction Contract (all 12 interaction rules); 02-RESEARCH.md Open Questions (swipe action width, error duration, SectionList performance).
  </read_first>

  <what-built>
    The full Categories feature is built and unit/component tested:
    - SectionList with two grouped sections (Expense/Income Categories), sticky headers
    - Per-group inline add inputs with duplicate detection and inline error display
    - Swipeable rows: grey 'In use' for categories with usage count > 0; red 'Delete' → Alert confirmation for unused categories
    - Live right-aligned usage counts (singular/plural/'—')
    - Per-group empty states with always-visible inline add
    - CategoriesProvider wraps the navigator in App.tsx
    - All TypeScript types resolve; jest full suite green; Metro bundle succeeds
  </what-built>

  <how-to-verify>
    1. Start the dev server: `npx expo start --tunnel` (or `npx expo start` if on the same LAN)
    2. Scan QR code with Expo Go on your phone (Android/iOS)
    3. Sign in with the seeded default account credentials
    4. Navigate to the **Categories** tab:
       - [ ] Both 'Expense Categories' and 'Income Categories' headers are visible
       - [ ] If no categories exist, each group shows its empty state ('No expense/income categories yet') with inline add inputs still visible
    5. **Add a category:**
       - [ ] Type "Groceries" in the Expense Categories inline input → press return or tap '+' → row appears immediately in the Expense group
       - [ ] Type "Salary" in the Income Categories input → row appears in the Income group
       - [ ] Try to add "groceries" (case-insensitive duplicate) → 'Already exists' error appears in danger below the input; clear on next keystroke
       - [ ] Try to add "" (blank) → nothing happens, no error
    6. **Verify usage counts:**
       - [ ] Newly added categories show "0 entries" right-aligned in the row
       - [ ] (If entries exist from Phase 3 testing or console seeding, count updates in real time)
       - [ ] During fast category adds, the SectionList scrolls smoothly
    7. **Swipe-to-delete:**
       - [ ] Swipe left on an unused category → red 'Delete' action panel reveals at 80px width
       - [ ] Tap 'Delete' → Alert shows 'Delete {name}? This cannot be undone.' → tap Cancel → alert dismisses, swipe retracts
       - [ ] Swipe again → tap Delete (confirm) → row removes from the list
       - [ ] Swipe left on a category with usage count > 0 → **grey** 'In use' panel shows instead of Delete → tapping it does nothing → tapping away retracts the swipe
    8. **Sticky headers:**
       - [ ] Add 10+ categories to one group → scroll the list → section headers stay pinned at the top
       - [ ] The inline add input scrolls with the sticky header — always accessible
    9. **Sign out test:**
       - [ ] Go to Account tab → sign out → sign back in → Categories tab still shows the same categories (Firestore data persists)
       - [ ] Usage counts still display correctly after sign-out/in cycle
    10. **Cross-platform (if both devices available):**
        - [ ] Repeat on the other platform — swipe behavior (friction, overshoot) feels platform-native on each
  </how-to-verify>

  <resume-signal>Type "approved" to complete the phase, or describe any issues (platform, behavior, visual)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | Category addDoc/deleteDoc writes cross this boundary; inline inputs accept user text that flows into Firestore document fields |
| gesture layer | Swipeable from react-native-gesture-handler processes touch input at the native layer — a compromised library could intercept gestures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information Disclosure | CategoriesOf query (inherited from queries.ts) | high | mitigate | Already mitigated in Phase 1 — categoriesOf applies `where("uid","==",uid)`; server-side rules reject unscoped reads |
| T-2-02 | Elevation of Privilege | addDoc payload in CategoriesScreen (via addCategory) | high | mitigate | addCategory in CategoriesProvider (Plan 02-01) hardcodes `uid: user.uid` — the screen has no direct Firestore access; server-side rules reject mismatched uid |
| T-2-03 | Tampering | Category name text injection via inline input | medium | mitigate | Category names rendered in `<Text numberOfLines={1}>` — React Native escapes text natively (no HTML rendering, no innerHTML); trimmed + case-normalized before comparison and storage |
| T-2-04 | Denial of Service | Race: delete while swipe panel revealed and entry added in Phase 3 | low | accept | CategoryInUse guard runs at deleteCategory call time — the window between swipe reveal (showing 'Delete') and tap is seconds; if an entry was added in that window, deleteCategory throws 'Category is in use' which the screen catches via `.catch()` and surfaces via Alert; risk accepted (narrow window, transient error, no data loss) |
| T-2-05 | Information Disclosure | Memory leak — onSnapshot listeners from provider | medium | mitigate | CategoriesProvider (Plan 02-01) cleanup callbacks verified in unit tests; the screen itself holds no subscriptions |
| T-2-06 | Tampering | Swipeable gesture interception by compromised native module | low | accept | react-native-gesture-handler (~2.32.0) is a first-party Expo Go bundled module (6.5M weekly downloads, maintained by Software Mansion, no postinstall scripts); RESEARCH Package Legitimacy Audit approved; risk accepted as negligible — the module is canonical React Native infrastructure |
| T-2-SC | Tampering | npm install of gesture-handler | high | mitigate | Installed via `npx expo install react-native-gesture-handler` — pins to ~2.32.0 (Expo Go bundled build), NOT npm latest 3.1.0; RESEARCH Package Legitimacy Audit verified: 8+ years, 6.5M weekly downloads, no postinstall scripts, inExpoGo: true |

</threat_model>

<verification>
Per-task automated gates:
- Task 1 (tracer): `npx tsc --noEmit && npx jest --ci --silent && npx expo export --platform android --output-dir .expo-check && npx expo-doctor`
- Task 2 (tests): `npx jest src/screens --ci --silent && npx jest --ci --silent && npx tsc --noEmit`
- Task 3 (checkpoint): Human verifies per instructions above

Full phase gate after checkpoint approval: `npx jest --ci --silent && npx tsc --noEmit && npx expo lint && npx expo-doctor` all green.
</verification>

<success_criteria>
- Categories tab renders two grouped sections with sticky headers, inline add inputs, Swipeable rows, and live usage counts — the Phase 1 placeholder is fully replaced
- CategoriesProvider wraps the navigator in App.tsx — provider mount order is AuthProvider (outer) > CategoriesProvider (inner)
- All CATS-01 through CATS-04 success criteria from ROADMAP verified on device (Task 3 checkpoint)
- Full jest suite (Phase 1 + 02-01 + 02-02) green — ≥63 + ≥10 + ≥13 = ≥86 tests passing
- TypeScript strict mode clean, Metro bundle succeeds, expo-doctor clean
- Device verification passed on both Android and iOS (or documented platform-specific issues)
</success_criteria>

<output>
Create `.planning/phases/02-categories/02-02-SUMMARY.md` when done
</output>
